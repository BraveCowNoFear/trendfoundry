# TrendFoundry 工作区

## 负责人决策

- 项目负责人：Codex，按勇敢牛牛授权自动推进。
- 当前方向：面向 B 站/YouTube AI 与开发者创作者的趋势情报包。
- 选择理由：2026 年创作者仍在购买省时间工作流，但平台正在打击低质量 AI 内容；有来源、有复现实测、有创作结构的情报包比批量内容更安全、更可卖。
- 当前 MVP：自动采集 GitHub、YouTube、Bilibili、Hacker News、arXiv 公开趋势信号，生成带评分的创作者选题包、日报 Markdown 和可筛选静态情报台。

## 外部证据样本

- Creator economy 2026 报告与行业文章普遍提到 AI 工具进入创作者生产流程，创作者自有产品与订阅是更稳的收入路径。
- YouTube 2026 相关报道强调会支持 AI 创作工具，但会继续压低质量、重复、欺骗性 AI 内容。
- GitHub topic `video-generation` 已有大量公开仓库，AI agent / video generation / creator workflow 是持续更新的技术内容池。
- Hacker News、arXiv、GitHub、YouTube 搜索页和 Bilibili 公共搜索接口都能提供无需账号/钱包前置依赖的公开趋势信号；其中 GitHub/Bilibili 可能限流或 412，需缓存兜底。

## 产品定义

TrendFoundry 每天/每周输出：

- 12 个有来源链接的创作者机会。
- 每个机会包含评分、受众、B 站标题、YouTube 标题、开场 hook、视频结构、缩略图提示词。
- 最高分机会可扩展成完整脚本、素材清单和演示步骤。

## 当前文件

- `package.json`：本地命令。
- `scripts/collect.mjs`：公开数据采集和机会评分。
- `scripts/build.mjs`：生成 Markdown brief 和静态站点。
- `scripts/server.mjs`：本地预览服务器。
- `scripts/run_daily.ps1`：Windows 日更脚本。
- `scripts/run_leads.ps1`：Windows 本地线索同步脚本。
- `scripts/export_pack.mjs`：生成买家可收取的样品包、机会 CSV、外联 CSV 和外联板。
- `scripts/sync_leads.mjs`：同步 GitHub Issue Form 线索到本地 CRM。
- `data/seed_sources.json`：初始查询主题。
- `data/latest.json`：最近一次采集结果，含 `errors` 与 cached fallback 标记。
- `docs/daily-brief.md`：当前可售卖样品。
- `docs/ready-to-record-script.md`：第一条可录视频脚本。
- `docs/public-sample.md`：公开免费样品，展示 3 条机会，不等于完整付费包。
- `docs/sales-page-copy.md`：支付页/销售页文案。
- `docs/launch-plan.md`：首发计划、客户画像、触达话术。
- `docs/outreach-playbook.md`：客户触达话术、分层和履约 SOP。
- `docs/outreach-board.md`：`npm run export` 生成的 20 人首发外联板。
- `docs/payment-platform-fields.md`：支付平台上架字段。
- `docs/design-system.md`：极简产品视觉标准。
- `docs/publishing.md`：GitHub 仓库与 Pages 发布说明。
- `docs/lead-capture.md`：GitHub Issue 线索入口和人工成交前处理流程。
- `docs/lead-pipeline.example.md`：本地 CRM 输出示例；真实 `docs/lead-pipeline.md` 不提交。
- `docs/lead-replies.md`：`npm run leads` 生成的本地回复草稿；真实文件可能含联系方式，不提交。
- `.github/workflows/pages.yml`：GitHub Pages 部署 `site/` 的 Actions workflow。
- `site/index.html`：展示页。
- `site/app.js`：站内搜索与来源筛选交互。
- `site/*.md`：供展示页按钮直接打开的样品交付物镜像。
- `site/public-sample.md` / `site/public-sample.csv`：无需登录即可查看/下载的公开样品入口。
- `dist/trendfoundry-sample-pack/`：`npm run export` 生成的可发送样品包，不纳入 Git。
- `记忆系统.md`：项目长期记忆与决策日志。

## 下一步队列

- GitHub 公开仓库：`https://github.com/BraveCowNoFear/trendfoundry`
- GitHub Pages 在线演示：`https://bravecownofear.github.io/trendfoundry/`
- 最近一次成功 Pages run：`27080995586`，提交 `0ec9d55 Publish free public sample pack`
- 样品包请求入口：`https://github.com/BraveCowNoFear/trendfoundry/issues/new?template=order-sample-pack.yml&title=Sample%20pack%20request%3A%20`
- 本地 CRM 同步：`npm run leads`，输出 `data/leads.json` 与 `docs/lead-pipeline.md`，两者因可能含联系方式而不提交。
- 线索回复草稿：`npm run leads` 同时输出 `docs/lead-replies.md`，含 GitHub/email 草稿；只供审核后发送，脚本不自动发消息。
- B 站 412 风控处理：已加入请求间隔、412/429/5xx 指数退避重试和 `collectionAttempts` 记录；后续如仍频繁失败，再补浏览器采集或官方/合规 RSS 源。
- 内容质量控制：已加入 `qualityFlags` / `qualityRisk`，对 engagement bait、overclaim、template hype、rights risk、too broad 条目扣分；展示组合优先 normal，再用 review 补位。
- 转化页结构：公开页已加入 3 档套餐（$9 sample issue、$19/mo weekly brief、$49/mo custom niche）和买家交付清单，仍保留 GitHub request 与 email order 双入口。
- 公开免费样品：已新增 `public-sample.md` 和 `public-sample.csv`，只展示 top 3 机会，用于降低买家首次信任门槛；完整 12 条和 CSV 仍作为付费/请求交付。
- 用 `dist/trendfoundry-sample-pack/prospects.csv` 做首批 20 个潜在客户个性化触达。
- 预览导出包：`PORT=4174 SITE_DIR=dist/trendfoundry-sample-pack npm start`，当前本地已用 `http://localhost:4174` 验证通过。
- 增加 B 站/YouTube 官方或合规 RSS/API 数据源。
- 增加一键生成完整视频脚本。
- 增加 Stripe/Gumroad/Ko-fi 等支付页文案和导入资产。
- 根据 `docs/launch-plan.md` 执行首发。
- 创建 GitHub 仓库后补充 Topics、英文 README、中文 README。
- 每天运行 `npm run daily` 生成新样品。
- Windows 计划任务：`TrendFoundryDaily`，每天自动刷新。
- Windows 计划任务：`TrendFoundryLeadSync`，每 2 小时独立同步 GitHub Issue 线索，状态 Ready。
- 最近一次脚本验证日志：`logs/daily-20260607-024137.log`。
- 最近一次线索同步日志：`logs/leads-20260607-033110.log`。
- 最近一次视觉闭环验证：本地 `http://localhost:4175`，桌面 1280px 与移动 390px 无横向溢出；公开 sample CTA 与 preview 区正常，`public-sample.md` / `.csv` 返回 200，Bilibili 筛选显示 3 张卡片。

## 安全边界

- 不做刷量、群发垃圾信息、伪造账号、规避平台审核或诱导性金融承诺。
- 注册、支付、实名、税务、钱包等账号门槛前的工作全部先做完；需要真实凭证时只生成待执行清单。
