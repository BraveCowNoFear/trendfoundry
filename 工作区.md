# TrendFoundry 工作区

## 负责人决策

- 项目负责人：Codex，按勇敢牛牛授权自动推进。
- 当前方向：面向 B 站/YouTube AI 与开发者创作者的趋势情报包。
- 选择理由：2026 年创作者仍在购买省时间工作流，但平台正在打击低质量 AI 内容；有来源、有复现实测、有创作结构的情报包比批量内容更安全、更可卖。
- 当前 MVP：自动采集 GitHub、YouTube、Bilibili、Hacker News、arXiv 公开趋势信号，生成带评分的创作者选题包、日报 Markdown 和可筛选静态情报台。
- 客户交付原则：客户看到和拿到的内容必须先给短路径；付费内容包第一文件固定为 `START-HERE.md`，长脚本、证据、审计只作为参考附件。

## 外部证据样本

- Creator economy 2026 报告与行业文章普遍提到 AI 工具进入创作者生产流程，创作者自有产品与订阅是更稳的收入路径。
- YouTube 2026 相关报道强调会支持 AI 创作工具，但会继续压低质量、重复、欺骗性 AI 内容。
- GitHub topic `video-generation` 已有大量公开仓库，AI agent / video generation / creator workflow 是持续更新的技术内容池。
- Hacker News、arXiv、GitHub、YouTube 搜索页和 Bilibili 公共搜索接口都能提供无需账号/钱包前置依赖的公开趋势信号；其中 GitHub/Bilibili 可能限流或 412，需缓存兜底。

## 产品定义

TrendFoundry 每天/每周输出：

- 12 个有来源链接的创作者机会。
- 每个机会包含评分、受众、B 站标题、YouTube 标题、开场 hook、视频结构、缩略图提示词。
- 最高分机会可扩展成完整脚本、素材清单和演示步骤。

## 当前文件

- `package.json`：本地命令。
- `scripts/collect.mjs`：公开数据采集和机会评分。
- `scripts/build.mjs`：生成 Markdown brief 和静态站点。
- `scripts/server.mjs`：本地预览服务器。
- `scripts/run_daily.ps1`：Windows 每日运营脚本，执行完整 `npm run operate` 流水线。
- `scripts/run_leads.ps1`：Windows 本地线索同步脚本。
- `scripts/run_ops.ps1`：Windows 本地一键运营流水线脚本。
- `scripts/export_pack.mjs`：生成买家可收取的样品包、机会 CSV、外联 CSV 和外联板。
- `scripts/export_commerce.mjs`：生成支付平台上架字段、SKU CSV/JSON 和手动 invoice 草稿。
- `scripts/report_ops.mjs`：生成本地老板视角运营报告，汇总公开部署、情报包、内容销售区块、线索、订单、外联和 commerce 状态。
- `scripts/fulfill_content_subscription.mjs`：按 subscriber/week/payment-ref 生成 $19/mo weekly pack 的 buyer-only 本地交付目录。
- `scripts/generate_content_subscription_crm.mjs`：读取 ignored subscriber status，生成 weekly subscription due queue、履约命令和公开安全说明。
- `scripts/fulfill_due_content_subscriptions.mjs`：读取 private due queue，批量准备所有 `prepare_delivery` weekly subscription buyer-only 周包。
- `scripts/generate_content_subscription_retention.mjs`：读取 private subscription CRM 队列，生成 renewal/payment-review 待审核草稿。
- `scripts/update_content_subscription_status.mjs`：外部确认订阅/付款后，写入或更新 ignored weekly subscriber status。
- `scripts/complete_content_subscription_delivery.mjs`：weekly pack 发出后，从本地订单 manifest 回写 ignored subscriber status 和下一期交付日期。
- `scripts/generate_social_asset.mjs`：用本机 Edge/Chrome 生成 `site/og-image.png` 社交预览图。
- `scripts/run_operations.mjs`：串联 daily、content-ops、commerce、leads、email intake/routing、subscription sync/due/retention、custom/standard email fulfillment、fulfill-ready、launch-assets、ops-report 和 QA 的本地运营流水线。
- `scripts/qa.mjs`：验证站点、交付边界、commerce、运营报告、计划任务和可选线上资源。
- `scripts/fulfill_order.mjs`：按买家/线索生成本地订单交付目录、交付邮件草稿和履约清单。
- `scripts/fulfill_content_order.mjs`：按外部付款确认生成内容包 buyer-only 本地订单目录、交付邮件草稿和履约清单。
- `scripts/complete_content_order_delivery.mjs`：内容订单人工发送后读取订单 manifest，确认 `START-HERE.md` + 四个参考文件存在，并把 ignored 内容销售 CRM 回写为 `fulfilled_waiting_feedback`。
- `scripts/complete_custom_proof_delivery.mjs`：custom proof pack 人工发送后验证 `custom-proof-pack.md`，写入 ignored completion receipt 和 `fulfilled_waiting_feedback`。
- `scripts/generate_custom_proof_pack.mjs`：按 niche/platform/buyer 生成 $49 custom proof pack 样张与本地交付草稿。
- `scripts/fulfill_ready_leads.mjs`：读取本地 CRM，为 `paid` 或批准样品的线索生成交付目录。
- `scripts/fulfill_email_orders.mjs`：读取本地邮件订单 intake，只为已付款订单生成 buyer-only 交付目录。
- `scripts/fulfill_custom_email_orders.mjs`：读取本地邮件订单 intake，只为已付款 custom-niche 订单生成 custom proof pack 交付目录。
- `scripts/sync_email_subscriptions.mjs`：把已付款 weekly email order 同步为 ignored subscription CRM rows。
- `scripts/audit_email_order_routing.mjs`：审计 email order 应走 payment reply、standard pack、weekly subscription 或 custom proof pack 哪条履约路径。
- `scripts/generate_content_outreach_review.mjs`：把每日 close queue 转成私密 reviewer-ready send packs，含 subject、正文、offer、follow-up date、审核 checklist 和 reply capture。
- `scripts/complete_content_outreach_send.mjs`：人工发送某个 reviewed send pack 后，写入 ignored send receipt 并把 CRM 回写为 `sent_waiting_reply`。
- `scripts/generate_content_outreach_send_log.mjs`：汇总 ignored outreach send receipts，生成公开计数和私密 send log。
- `scripts/generate_content_deal_desk.mjs`：读取私密 replies/CRM deal stage，生成成交回复、invoice 草稿、付款确认后履约命令和异议 playbook。
- `scripts/generate_content_evidence_pack.mjs`：把当前脚本和 episode queue 转成买家可用的 source URL、claim checklist、proof asset、verification step 和 limitation evidence pack。
- `scripts/generate_content_delivery_gate.mjs`：检查 buyer content pack 的 `START-HERE.md`、四个参考文件、seller-only 边界、敏感数据请求、高风险结果承诺、精简可读性和 mojibake marker，失败则阻止内容侧流水线继续。
- `scripts/update_content_sale_status.mjs`：用命令行 upsert ignored 内容销售 CRM 状态和 reply 摘要，避免手工编辑 `overrides.csv` / `replies.csv`。
- `scripts/generate_content_customer_success.mjs`：读取 ignored 内容销售 CRM 与交付完成回执，生成私密反馈/testimonial/upsell 跟进草稿和公开安全计数。
- `scripts/update_content_testimonial_status.mjs`：用命令行 upsert ignored testimonial permission / quote 状态。
- `scripts/generate_content_testimonials.mjs`：读取 ignored testimonial rows，生成私密 social proof review queue 和公开计数。
- `scripts/draft_outreach.mjs`：从最新公开信号生成本地一对一外联草稿包。
- `scripts/sync_leads.mjs`：同步 GitHub Issue Form 线索到本地 CRM。
- `data/seed_sources.json`：初始查询主题。
- `data/latest.json`：最近一次采集结果，含 `errors` 与 cached fallback 标记。
- `docs/daily-brief.md`：当前可售卖样品。
- `docs/ready-to-record-script.md`：第一条可录视频脚本。
- `docs/public-sample.md`：公开免费样品，展示 3 条机会，不等于完整付费包。
- `docs/sales-page-copy.md`：支付页/销售页文案。
- `docs/launch-plan.md`：首发计划、客户画像、触达话术。
- `docs/outreach-playbook.md`：客户触达话术、分层和履约 SOP。
- `docs/outreach-board.md`：`npm run export` 生成的 20 人首发外联板。
- `docs/outreach-drafts.md`：本地一对一外联草稿生成说明。
- `docs/payment-platform-fields.md`：支付平台上架字段。
- `docs/commerce.md`：commerce listing 生成命令、SKU 和上架安全边界。
- `docs/ops-report.md`：本地运营报告命令和字段说明。
- `docs/social-assets.md`：社交预览图生成命令和视觉规则。
- `docs/operations.md`：本地一键运营流水线命令、日志和安全边界。
- `docs/qa.md`：本地/线上 QA 命令和覆盖范围。
- `docs/design-system.md`：极简产品视觉标准。
- `docs/publishing.md`：GitHub 仓库与 Pages 发布说明。
- `docs/lead-capture.md`：GitHub Issue 线索入口和人工成交前处理流程。
- `docs/lead-pipeline.example.md`：本地 CRM 输出示例；真实 `docs/lead-pipeline.md` 不提交。
- `docs/lead-replies.md`：`npm run leads` 生成的本地回复草稿；真实文件可能含联系方式，不提交。
- `docs/fulfillment.md`：订单履约命令、买家交付文件和 seller-only 文件边界。
- `docs/content-fulfillment.md`：内容包履约命令、买家交付文件和安全边界。
- `docs/content-subscription-fulfillment.md`：weekly subscription 逐周履约命令、买家交付文件和安全边界。
- `docs/content-subscription-crm.md`：weekly subscription 本地 CRM 说明；真实 subscriber queue 留在 ignored `dist/content-subscription-crm/` 与 `data/content-subscriptions/`。
- `docs/custom-proof-pack.md`：默认 custom proof pack 样张，可用 `--niche` / `--platform` / `--buyer` 重新生成。
- `docs/content-sales-sequence.md`：内容产品销售/跟进草稿序列；只供审核后发送或发布。
- `docs/content-prospecting.md`：内容产品 first-customers 线索生成说明；真实 prospect board 和草稿留在 ignored `dist/content-prospecting/`。
- `docs/content-sales-crm.md`：内容产品本地销售 CRM 说明；真实 pipeline 留在 ignored `dist/content-sales-crm/`。
- `docs/content-subscription-plan.md`：$19/mo weekly proof pack 的 4 周交付日历和订阅者邮件草稿。
- `docs/content-revenue-model.md`：内容产品收入模型、MRR target 和每周销售动作量；只作 planning model，不作收益承诺。
- `docs/content-feedback-loop.md`：内容销售反馈学习闭环；读取本地 CRM 和可选私密 reply 摘要，生成 product learnings 与下一轮反馈问题。
- `docs/content-close-pack.md`：内容产品每日成交包说明；真实五条 close queue 留在 ignored `dist/content-close-pack/`。
- `docs/content-outreach-review.md`：内容外联审核包说明；真实 prospect reviewer send packs 留在 ignored `dist/content-outreach-review/`。
- `docs/content-deal-desk.md`：内容成交处理台说明；真实回复、invoice 草稿和履约命令留在 ignored `dist/content-deal-desk/`。
- `docs/content-health-gate.md`：内容文本健康门禁；用 UTF-8 读文件检查真实 mojibake、Bilibili 中文可读性和公开文档泄露边界。
- `.github/workflows/pages.yml`：GitHub Pages 部署 `site/` 的 Actions workflow。
- `.github/workflows/daily-ops.yml`：GitHub Actions 云端每日运营流水线，运行 `npm run operate`、提交刷新结果，并直接部署 `site/` 到 Pages。
- `site/index.html`：展示页。
- `site/order/index.html`：无登录下单页，集中展示三档套餐、英文/中文邮件草稿和安全边界。
- `site/app.js`：站内搜索、来源筛选与中英双语切换交互。
- `site/*.md`：供展示页按钮直接打开的样品交付物镜像。
- `site/public-sample.md` / `site/public-sample.csv`：无需登录即可查看/下载的公开样品入口。
- `site/topics/*.html`：自动生成的 SEO 长尾主题页，覆盖 AI video ideas、GitHub AI projects、Bilibili AI topics、YouTube AI workflows 和 developer trend brief。
- `site/feed.xml` / `site/feed.json`：RSS 与 JSON Feed，发布 top 12 当前机会，服务订阅和复访。
- `site/issues/*.html`：公开 issue 归档页；`latest.html` 指向最新期，`index.html` 展示归档索引。
- `docs/issues/*.md`：公开 issue 的 Markdown 镜像，便于仓库访客直接阅读。
- `site/robots.txt` / `site/sitemap.xml`：公开搜索索引入口，指向首页、样品、脚本、销售页、Feed 和 SEO 主题页。
- `dist/trendfoundry-sample-pack/`：`npm run export` 生成的可发送样品包，不纳入 Git。
- `记忆系统.md`：项目长期记忆与决策日志。

## 下一步队列

- GitHub 公开仓库：`https://github.com/BraveCowNoFear/trendfoundry`
- GitHub Pages 在线演示：`https://bravecownofear.github.io/trendfoundry/`
- GitHub Pages 中文入口：`https://bravecownofear.github.io/trendfoundry/zh/`
- 最近一次成功 Pages run：`27096347501`，提交 `6ff4a46 Add paid email order fulfillment workflow`
- GitHub Topics：`ai-tools`、`ai-video`、`bilibili`、`creator-economy`、`creator-tools`、`github-trending`、`json-feed`、`newsletter`、`rss`、`static-site`、`trend-intelligence`、`youtube`
- 样品包请求入口：`https://github.com/BraveCowNoFear/trendfoundry/issues/new?template=order-sample-pack.yml&title=Sample%20pack%20request%3A%20`
- 本地 CRM 同步：`npm run leads`，输出 `data/leads.json` 与 `docs/lead-pipeline.md`，两者因可能含联系方式而不提交。
- 线索回复草稿：`npm run leads` 同时输出 `docs/lead-replies.md`，含 GitHub/email 草稿；只供审核后发送，脚本不自动发消息。
- GitHub Issue Form 线索入口：已包含 creator channel、platform、pack type、preferred contact、preferred delivery route、niche preference 和 safety acknowledgement；表单/回复草稿明确 scene-by-scene ready-to-record script，不要求公开提交支付卡、密码、private ID 或敏感信息。
- 订单履约：`npm run fulfill -- --buyer="..." --contact="..." --channel="..." --type="sample-issue"`，输出 `dist/orders/<order-id>/`，只包含买家交付文件与本地邮件草稿。
- 内容包履约：`npm run buyer-pack && npm run fulfill-content -- --buyer="..." --contact="..." --channel="..." --product="trendfoundry-proof-script-pack" --payment-ref="external-confirmation-id"`，输出 `dist/content-orders/<order-id>/`，只包含 `START-HERE.md`、`full-episode-script.md`、`episode-workbench.md`、`content-evidence-pack.md`、`content-editorial-audit.md` 和本地操作草稿；不发送消息、不收款、不上传。
- 内容订单完成回写：`npm run complete-content-order-delivery -- --order="dist/content-orders/<order-id>" --source="youtube" --creator="..." --next-due="YYYY-MM-DD"`，确认 `START-HERE.md` + 四个参考文件存在后写入 ignored CRM `fulfilled_waiting_feedback`。
- 内容客户成功跟进：`npm run content-customer-success` 读取 `fulfilled_waiting_feedback` CRM 行和 `dist/content-order-completion/` 回执，生成 ignored `dist/content-customer-success/followup-drafts.md`；公开 `docs/content-customer-success.md` 只显示计数和安全边界。
- 内容 testimonial 记录：`npm run record-content-testimonial -- --source="youtube" --creator="..." --quote="..." --permission="explicit" --stage="publish_ready" --context="After proof script delivery"` 写入 ignored `data/content-sales-crm/testimonials.csv`。
- 内容 testimonial bank：`npm run content-testimonials` 生成 ignored `dist/content-testimonials/testimonial-bank.md` / `publish-candidates.md`；公开 `docs/content-testimonials.md` 只显示计数和安全边界。
- custom proof pack 交付完成回写：`npm run complete-custom-proof-delivery -- --order="dist/custom-proof-pack" --source="custom-email" --creator="..." --next-due="YYYY-MM-DD"`；也支持 `--order="dist/custom-email-orders/<order-id>"`。
- 内容交付门禁：`npm run content-delivery-gate` 输出 `docs/content-delivery-gate.md` 和 ignored `dist/content-delivery-gate/`；付费内容包发送前必须 0 failed checks。
- weekly 内容订阅履约：`npm run content-subscription && npm run fulfill-content-subscription -- --subscriber="..." --contact="..." --channel="..." --week="1" --payment-ref="external-confirmation-id"`，输出 `dist/content-subscriptions/<order-id>/`，只包含 `weekly-proof-pack.md`、`subscriber-email.md` 和本地操作文件；不发送消息、不收款、不上传。
- weekly 内容订阅 CRM：`npm run content-subscription-crm` 读取 ignored `data/content-subscriptions/subscribers.csv`，输出公开 `docs/content-subscription-crm.md` 与 ignored `dist/content-subscription-crm/due-queue.md` / `fulfillment-commands.ps1`；用于判断本周谁可交付、谁需付款复核、谁需续费检查。
- weekly 到期批量准备：`npm run content-subscription-due` 读取 ignored due queue，为所有 `prepare_delivery` 行生成 `dist/content-subscriptions/<order-id>/` 周包，并输出公开计数 `docs/content-subscription-due.md`。
- weekly 留存/续费草稿：`npm run content-subscription-retention` 读取 ignored due queue，为 `payment_review_before_delivery` 与 `renewal_check` 行生成 ignored `dist/content-subscription-retention/drafts.md`，公开 `docs/content-subscription-retention.md` 只显示计数。
- weekly 订阅状态记录：`npm run record-content-subscription -- --subscriber="..." --contact="..." --channel="..." --payment-ref="external-confirmation-id"` 写入 ignored `data/content-subscriptions/subscribers.csv`，供 `content-subscription-crm` 自动排队。
- weekly 订阅交付完成回写：`npm run complete-content-subscription-delivery -- --order="dist/content-subscriptions/<order-id>"` 读取订单 manifest，更新 `last_delivered_week`、`last_sent_at` 与 `next_delivery_date`。
- $49 定制内容包：`npm run custom-proof-pack -- --niche="ComfyUI workflow creators" --platform="Bilibili" --buyer="..." --channel="..."`，输出 `docs/custom-proof-pack.md` 与 ignored `dist/custom-proof-pack/`；buyer deliverable 默认为 `custom-proof-pack.md`，可选附加普通内容三件套。
- 内容销售序列：`npm run content-sales` 输出 `docs/content-sales-sequence.md` 与 ignored `dist/content-sales-sequence/`，包含 6 条待审核草稿，不发送、不发帖、不收款。
- 内容 prospecting：`npm run content-prospects` 输出 `docs/content-prospecting.md` 和 ignored `dist/content-prospecting/`，生成 20 条本地 prospect board / CSV / 个性化草稿，不自动发送、不抓私密联系方式。
- 内容销售 CRM：`npm run content-crm` 读取 ignored prospecting 输出与可选 `data/content-sales-crm/` 私密状态覆盖，生成 `docs/content-sales-crm.md` 和 ignored `dist/content-sales-crm/`；用于每日 5 个一对一审核/跟进。
- 内容销售状态回写：`npm run record-content-sale -- --source="..." --creator="..." --stage="sent_waiting_reply" --next-due="YYYY-MM-DD" --notes="..."` 写入 ignored `data/content-sales-crm/overrides.csv`；带 `--summary` / `--objection` 时同步 upsert ignored `replies.csv`。
- 内容订阅计划：`npm run content-subscription` 输出 `docs/content-subscription-plan.md` 和 ignored `dist/content-subscription-plan/`，生成 4 周 weekly pack 交付日期、主题、proof episode 和 subscriber email 草稿。
- 内容收入模型：`npm run content-revenue` 输出 `docs/content-revenue-model.md` 和 ignored `dist/content-revenue-model/`，基于 product listing、CRM、prospecting 和 subscription plan 生成保守/基准/乐观情景。
- 内容反馈学习：`npm run content-feedback` 输出 `docs/content-feedback-loop.md` 和 ignored `dist/content-feedback-loop/`，读取可选 ignored `data/content-sales-crm/replies.csv`，把回复/异议转成 learnings 与下一轮问题；默认不公开私密回复。
- 内容每日成交包：`npm run content-close` 输出 `docs/content-close-pack.md` 和 ignored `dist/content-close-pack/`，从 CRM/feedback/product listing 生成 5 条待人工审核的 close queue、草稿和 reply capture 模板。
- 内容外联审核包：`npm run content-outreach-review` 输出公开 `docs/content-outreach-review.md` 与 ignored `dist/content-outreach-review/`，把每日 close queue 转成逐个审核的 send packs；脚本不发送、不发帖、不收款、不公开 prospect 行。
- 内容外联发送回执：`npm run complete-content-outreach-send -- --review-id="outreach-01-example"` 记录已人工发送的 reviewed send pack，写 ignored `dist/content-outreach-sends/` 并回写 CRM `sent_waiting_reply`；`npm run content-outreach-sends` 只刷新公开计数和私密 send log。
- 内容回复 intake：把复制的 creator/buyer 回复放入 ignored `data/content-sales-crm/reply-inbox/*.txt|md|eml` 后运行 `npm run content-reply-intake`，自动 upsert ignored `replies.csv` / `overrides.csv`，再由 CRM/deal desk 接管。
- 内容成交处理台：`npm run content-deal-desk` 输出公开 `docs/content-deal-desk.md` 与 ignored `dist/content-deal-desk/`，把私密 reply / paid stage 转成 reviewer-ready 回复、invoice 草稿和付款确认后的履约命令；脚本不发送、不收款、不履约。
- 内容健康门禁：`npm run content-health` 输出 `docs/content-health-gate.md` 和 ignored `dist/content-health-gate/`，检查 UTF-8 文本、Bilibili 可读中文、mojibake marker 和 public close doc 泄露。
- 线索履约：`npm run leads && npm run fulfill-ready`，为 `paid` 线索生成本地订单；`-- --include-qualified` 只用于人工批准的样品交付。
- 邮件订单 intake：把 copied buyer email 的 `.txt`、`.md` 或 `.eml` 放入 ignored `data/email-orders/` 后运行 `npm run intake-email-orders`，生成本地订单管线和付款回复包。
- 邮件订单路由审计：`npm run audit-email-order-routing` 读取 `dist/email-order-intake/orders.json`，输出公开 `docs/email-order-routing.md` 与 ignored `dist/email-order-routing/routes.csv`，证明 sample/weekly/custom/unpaid 各走正确 handler。
- 邮件 weekly 订阅同步：已付款 weekly email order 进入 `dist/email-order-intake/orders.json` 后，运行 `npm run sync-email-subscriptions` 写入 ignored `data/content-subscriptions/subscribers.csv`，再由 subscription CRM/due/retention 接管。
- 已付款邮件订单履约：外部确认付款后，让订单文本包含 `Paid: yes`、`Payment confirmed` 或 `Stage: paid`，运行 `npm run fulfill-email-orders` 生成 buyer-only `dist/orders/<order-id>/`。
- weekly email order 不走普通 `fulfill-email-orders`：已付款 `tier=weekly-brief` 由 `sync-email-subscriptions` + `content-subscription-due` 生成周包；普通 email fulfillment 会跳过并记录 `weekly_subscription_handled_by_content_subscription_due`。
- custom email order 不走普通 `fulfill-email-orders`：已付款 `tier=custom-niche` 由 `npm run fulfill-custom-email-orders` 生成 `dist/custom-email-orders/<order-id>/custom-proof-pack.md`；普通 email fulfillment 会跳过并记录 `custom_order_handled_by_fulfill_custom_email_orders`。
- 一对一外联草稿：`npm run daily` 会在日更末尾自动刷新；也可单独运行 `npm run draft-outreach` 输出 `dist/outreach-drafts/`；只生成本地待审核草稿，不自动发送。
- 首发传播资产：`npm run launch-assets` 输出 `dist/launch-assets/launch-posts.md`、`.csv` 和可提交的 `docs/launch-posts.md`，覆盖 X、LinkedIn、Bilibili dynamic、GitHub pinned comment、warm email；状态均为 draft/review，不自动发布或发送。
- 上架资产：`npm run commerce` 输出 `dist/commerce/`，包含三档 SKU、平台字段、CSV/JSON 和手动 invoice 草稿。
- 运营报告：`npm run ops-report` 输出 `dist/ops-report/`，汇总产品、内容销售区块、线索、订单、外联、上架资产和下一步动作。
- 社交预览：`npm run social && npm run build` 生成 `site/og-image.png` 并在站点 head / 页面视觉区引用，用于外联、checkout 和社交链接预览。
- SEO 长尾页：`npm run build` 自动生成 `site/topics/*.html`、`robots.txt` 和 `sitemap.xml`；首页 Search pages 区块链接到这些入口，主题页保留样品/脚本/下单 CTA。
- 订阅入口：`npm run build` 自动生成 `site/feed.xml` 与 `site/feed.json`；首页 Subscribe 区块链接 RSS/JSON Feed，把一次访问变成可复访订阅。
- 公开归档：`npm run build` 自动生成 `site/issues/<date>.html`、`site/issues/latest.html`、`site/issues/index.html` 和 `docs/issues/<date>.md`；云端 daily-ops 显式 `git add docs/issues site/issues`，确保新增日期 issue 能被自动提交。
- 一键运营：`npm run operate` 串联刷新产品、commerce、线索、ready 订单、ops-report 和 `qa --skip-scheduler`；不发送消息、不收款、不上传、不改标签。
- QA 守门：`npm run qa` 做本地不变量检查；`npm run qa -- --online` 额外验证公开 Pages 资源。
- B 站 412 风控处理：已加入请求间隔、412/429/5xx 指数退避重试和 `collectionAttempts` 记录；后续如仍频繁失败，再补浏览器采集或官方/合规 RSS 源。
- 内容质量控制：已加入 `qualityFlags` / `qualityRisk`，对 engagement bait、overclaim、template hype、rights risk、too broad 条目扣分；展示组合优先 normal，再用 review 补位。
- 转化页结构：公开页已加入 3 档套餐（$9 sample issue、$19/mo weekly brief、$49/mo custom niche）和买家交付清单，仍保留 GitHub request 与 email order 双入口。
- 公开免费样品：已新增 `public-sample.md` 和 `public-sample.csv`，只展示 top 3 机会，用于降低买家首次信任门槛；完整 12 条和 CSV 仍作为付费/请求交付。
- 用 `dist/trendfoundry-sample-pack/prospects.csv` 做首批 20 个潜在客户个性化触达。
- 预览导出包：`PORT=4174 SITE_DIR=dist/trendfoundry-sample-pack npm start`，当前本地已用 `http://localhost:4174` 验证通过。
- 增加 B 站/YouTube 官方或合规 RSS/API 数据源。
- 增加一键生成完整视频脚本。
- 增加 Stripe/Gumroad/Ko-fi 等支付页文案和导入资产。
- 根据 `docs/launch-plan.md` 执行首发。
- 创建 GitHub 仓库后补充 Topics、英文 README、中文 README。
- 每天运行 `npm run daily` 生成新样品。
- Windows 计划任务：`TrendFoundryDaily`，每天 08:15 自动运行完整 `npm run operate` 流水线，状态 Ready。
- Windows 计划任务：`TrendFoundryLeadSync`，每 2 小时独立同步 GitHub Issue 线索，状态 Ready。
- 最近一次脚本验证日志：`logs/daily-20260607-024137.log`。
- 最近一次线索同步日志：`logs/leads-20260607-033110.log`。
- 最近一次公开验证：`https://bravecownofear.github.io/trendfoundry/?v=6ff4a46`、`zh/`、`order/`、`public-sample.md`、`public-sample.en.md`、`public-sample.zh-CN.md`、`public-sample.csv`、`public-sample.en.csv`、`public-sample.zh-CN.csv`、`ready-to-record-script.md`、`sitemap.xml`、`feed.xml`、`feed.json`、`issues/`、`issues/latest.html`、`issues/2026-06-07.html`、`topics/ai-video-ideas.html`、`og-image.png` 均返回 200；Pages run `27096347501` 成功。
- 最近一次视觉闭环验证：Codex 内置浏览器线上渲染首页和 `/order/`，桌面 1280px 与移动 390px 无横向溢出；首页和订单页各只有 1 个 H1，订单页 8 个 mailto CTA 正常；截图记录为 `dist/qa/online-email-fulfillment-*-viewport-6ff4a46.png`。
- 最近一次履约验证：`npm run fulfill -- --order-id="qa-test-sample-2"` 生成本地订单目录；不含 `prospects.csv`、`outreach-board.md`、`latest.json`；UTF-8 中文正常。
- 最近一次买家交付边界增强：`dist/trendfoundry-sample-pack/manifest.json` 已拆分 `buyerDeliverables` 与 `sellerOnlyFiles`；订单 `manifest.json` 增加 `primaryValue`；`delivery-email.md` 明确 `ready-to-record-script.md` 是 6-8 分钟 scene-by-scene 脚本。临时订单 `qa-delivery-copy-check` 已抽查并清理。
- 最近一次外联草稿验证：`npm run daily` 已串联生成 20 份本地一对一草稿；状态为 `drafted_review_before_sending`，包含公开样品链接，不含自动发送动作或收益承诺。
- 最近一次线索履约验证：`npm run fulfill-ready` 真实空线索路径生成 0 订单报告；临时 paid/qualified 测试生成 2 个订单、跳过 1 个 new lead，订单无 seller-only 文件。
- 最近一次上架资产验证：`npm run commerce` 生成 3 个 SKU；JSON/Markdown/CSV/手动 invoice 均不把 `prospects.csv`、`outreach-board.md`、`latest.json` 列为买家交付；商品文案已明确包含 scene-by-scene ready-to-record script，销售文案为 Markdown/CSV/HTML/脚本交付。
- 最近一次公开 sales copy 验证：`https://bravecownofear.github.io/trendfoundry/sales-page-copy.md?v=d01be0f` 返回 200；无 `prospects.csv` 买家交付项，无 PDF 承诺。
- 最近一次运营报告验证：`npm run ops-report` 输出 source errors 1、lead count 0、outreach drafts 20、commerce products 3、email fulfillment prepared 0、prepared order directories 0；报告包含 latest online QA 178/178、latest QA run 178/178 和最新 operate 步骤；QA 测试订单目录已清理。
- 最近一次社交预览验证：`npm run social && npm run build` 生成 1200x630 `site/og-image.png`；线上 `og-image.png?v=7a844d7` 返回 200；OG/Twitter meta 存在；Edge 桌面 1280px 与移动 390px 无横向溢出，预览图加载正常。
- 最近一次一键运营验证：`npm run operate` 成功串联 daily、commerce、leads、intake-email-orders、fulfill-email-orders、fulfill-ready、launch-assets、ops-report 和 QA；采集 125 条、source errors 1、线索 0、email intake 0、email fulfillment 0、ready 订单 0、外联草稿 20、launch asset files 2、commerce SKU 3；末尾 `qa --skip-scheduler` 138/138 通过；未发送消息、未收款、未上传、未改 GitHub 标签。
- 成片脚本交付物：`ready-to-record-script.md` 已从短提纲升级为 6-8 分钟 scene-by-scene 脚本，包含 demo checklist、recording outline、asset checklist、shorts hooks、publishing metadata 和 fact safety notes；站点首屏 `Open script` 入口已视觉复验。
- 最近一次计划任务验证：手动触发 `schtasks /Run /TN TrendFoundryDaily` 成功；任务回到 Ready，Last Result 0；日志 `logs/daily-ops-20260607-045135.log` 生成。
- 最近一次 QA 守门验证：`npm run qa -- --online` 178/178 通过，覆盖本地脚本、GitHub Issue Form、lead reply 文案、launch assets、站点、双语首页控件、静态中文入口、无登录下单页、双语 public sample、脚本交付物、OG 图、seller-only 边界、commerce scene-by-scene 文案、sample pack manifest 分类、临时履约订单/邮件、email order intake、paid email fulfillment、ops report QA gate、Windows 计划任务、GitHub Actions operations workflow、SEO 主题页、Feed、公开 issue 归档、sitemap/robots 和公开 Pages 资源；最新线上版本为 `6ff4a46`。
- 最近一次 SEO 获客资产验证：`npm run build` 生成 5 个主题页、`robots.txt` 和 `sitemap.xml`；`npm run qa` 80/80 通过，`npm run qa -- --online` 95/95 通过；Python Playwright + Edge 本地与线上视觉闭环确认首页与 `topics/ai-video-ideas.html` 在 1280px/390px 下无横向溢出，首页 5 个 SEO topic links 可见，主题页 8 张卡和 Request/sample/script CTA 可见，截图在 `dist/qa/local-seo-py-*.png` 与 `dist/qa/online-seo-*-f1486f6.png`。
- 最近一次订阅资产验证：修复 cached fallback 文案重复追加问题；`npm run operate` 成功刷新 126 条机会、导出样品包、外联草稿、commerce、launch assets、ops-report，并通过 `qa --skip-scheduler` 83/83；完整 `npm run qa` 91/91 通过，线上 `npm run qa -- --online` 111/111 通过。`site/feed.xml` 与 `site/feed.json` 均含 12 条机会和订单 CTA；Python Playwright + Edge 本地与线上视觉闭环确认首页 Subscribe 区块在 1280px/390px 下可见、RSS/JSON Feed 按钮存在且无横向溢出，截图在 `dist/qa/local-feed-*.png` 与 `dist/qa/online-feed-*-a6a263e.png`。
- 最近一次公开归档验证：`npm run build` 生成 `docs/issues/2026-06-07.md`、`site/issues/2026-06-07.html`、`site/issues/latest.html` 和 `site/issues/index.html`；`npm run operate` 通过 `qa --skip-scheduler` 93/93，完整 `npm run qa` 98/98，线上 `npm run qa -- --online` 123/123 通过。Python Playwright + Edge 本地与线上视觉闭环确认首页 Archive 区块、issue 页 12 张卡、archive index 在 1280px/390px 下无横向溢出，截图在 `dist/qa/local-issue-*.png` 与 `dist/qa/online-issue-*-860f10d.png`。
- 最近一次双语转化页验证：`f0d66d1 Add bilingual landing page toggle` 已上线；首页新增 EN/中文切换，覆盖首屏、套餐、样品、交付清单、SEO 入口、Feed、Archive、搜索/筛选计数、卡片摘要、制作大纲和 CTA。`npm run qa` 103/103 通过，`npm run qa -- --online` 128/128 通过；Python Playwright + Edge 本地和线上确认桌面 1280px/移动 390px 无横向溢出，中文切换持久化、刷新后仍为中文，0 搜索结果显示 `0 个可见机会`，截图在 `dist/qa/local-i18n-home-*.png` 与 `dist/qa/online-i18n-home-*-f0d66d1.png`。
- 最近一次中文静态入口与首屏 polish 验证：`09e6c34 Add static Chinese landing page` 新增 `/zh/`，`7becba6 Polish landing page source mix messaging` 优化首页/中文入口的 source mix 面板和周更情报文案，`8df6662 Simplify source mix legend` 将首屏来源组合收敛成更安静的文字 legend；`npm run qa` 109/109 通过，`npm run qa -- --online` 138/138 通过。Python Playwright + Edge 本地和线上确认桌面 1280px/移动 390px 无横向溢出，`/zh/` 默认中文，source mix legend 渲染正常，Bilibili 筛选只显示 Bilibili 卡片，0 搜索结果显示 `0 个可见机会`，OG 预览图加载正常，截图在 `dist/qa/local-source-legend-*.png` 与 `dist/qa/online-source-legend-*-8df6662.png`。
- 最近一次双语样品资产验证：`64507a5 Add bilingual public sample assets` 新增 `public-sample.en.md`、`public-sample.zh-CN.md`、`public-sample.en.csv`、`public-sample.zh-CN.csv`，并把样品包 manifest、launch assets、commerce/fulfillment 文案和 `/zh/` CTA 同步到双语样品入口；`44472ef Clean English public sample titles` 清理英文样品中的中文标题混入。`npm run qa` 114/114 通过，`npm run qa -- --online` 145/145 通过。Python Playwright + Edge 线上确认 `/zh/` 在 1280px/390px 无横向溢出，中文/英文样品链接存在；HTTP 检查确认英文样品不含中文“为什么”标题，截图在 `dist/qa/online-bilingual-sample-zh-*-64507a5.png`。
- 最近一次无登录下单页验证：`78aff21 Add no-login order page` 新增 `site/order/index.html`，首页和 `/zh/` 均链接到 `/order/`；页面包含三档套餐、英文/中文 mailto 草稿、GitHub 备选入口和不收集公开支付信息的安全说明。`npm run qa` 121/121 通过，`npm run qa -- --online` 155/155 通过；Python Playwright + Edge 本地和线上确认 `/order/` 在 1280px/390px 无横向溢出、3 个 tier、2 个邮件草稿、至少 6 个 mailto CTA 和安全文案均正常，截图在 `dist/qa/local-order-*.png` 与 `dist/qa/online-order-*-78aff21.png`。

## 安全边界

- 不做刷量、群发垃圾信息、伪造账号、规避平台审核或诱导性金融承诺。
- 注册、支付、实名、税务、钱包等账号门槛前的工作全部先做完；需要真实凭证时只生成待执行清单。

## 2026-06-07 付款回复草稿闭环

- 新增 `scripts/draft_payment_reply.mjs` 与 `npm run payment-reply`：收到无登录邮件订单后，可按 tier/buyer/contact/channel/niche/delivery-route 生成本地 `dist/payment-replies/<order-id>/`。
- 生成内容包括 `payment-reply.md`、`invoice-draft.md`、`payment-checklist.md` 和 `manifest.json`；脚本只写本地草稿，不发送邮件、不创建支付链接、不收款、不上传文件、不改 GitHub 状态。
- 安全边界：付款回复明确不得要求买家通过邮件或公开 issue 发送 card numbers、private IDs、passwords、wallet seeds 或 payment credentials；buyer delivery 仍只列 `daily-brief.md`、`ready-to-record-script.md`、`opportunities.csv` 和 public sample 文件，排除 `prospects.csv`、`outreach-board.md`、`latest.json`。
- `/order/` 已增加 “Payment reply” 说明区，解释下一封付款说明邮件会本地生成并人工审核；移动端修复标题/副标题宽度，改为跟随父容器 100% 而不是 `100vw - 48px`，避免 headless/mobile 裁切。
- 本地验证：`node --check` 覆盖 `draft_payment_reply.mjs`、`qa.mjs`、`report_ops.mjs`、`build.mjs`；`npm run build` 成功构建 12 cards；`npm run payment-reply -- --order-id="qa-demo-weekly" ...` 成功生成样例包；`npm run qa` 129/129 通过。
- 视觉闭环：Edge CDP 明确设置桌面 1280x1100 与移动 390x1100 viewport，`/order/` 的 `scrollWidth` 等于 `clientWidth`（桌面 1265/1265，移动 390/390），`hasPaymentReply=true`；截图为 `dist/qa/cdp-payment-order-desktop.png` 与 `dist/qa/cdp-payment-order-mobile.png`。
- 线上验证：提交 `32879d1 Add payment reply draft workflow`，Pages run `27090835761` 成功；`npm run qa -- --online` 164/164 通过。公开 `/order/?v=32879d1` 经 Edge CDP 桌面 1280x1100 与移动 390x1100 复验，`scrollWidth=clientWidth`、`hasPaymentReply=true`、`hasInvoiceReference=true`、`clipped=false`；截图为 `dist/qa/online-payment-order-desktop-32879d1.png` 与 `dist/qa/online-payment-order-mobile-32879d1.png`。

## 2026-06-07 本地邮件订单 intake 自动化

- 新增 `scripts/lib/payment_reply.mjs`，把付款回复生成逻辑抽成可复用库；`scripts/draft_payment_reply.mjs` 保持 `npm run payment-reply` CLI 行为不变。
- 新增 `scripts/intake_email_orders.mjs` 与 `npm run intake-email-orders`：扫描 ignored `data/email-orders/` 中的 `.txt`、`.md`、`.eml` 本地订单文本，解析 tier、buyer、contact、channel、niche、delivery route，并生成 `dist/email-order-intake/orders.json`、`pipeline.md` 以及匹配的 `dist/payment-replies/<order-id>/`。
- 已将 `intake-email-orders` 接入 `npm run operate`，每日运营流水线会自动扫描本地订单文本；没有订单时输出空管线，不连接邮箱、不发送邮件、不创建支付链接、不收款、不上传文件、不改 GitHub 状态。
- 修复 `scripts/run_operations.mjs` 的运营报告滞后一轮问题：现在每个 step 完成后都会持久化 `dist/ops-run/latest-run.json`，因此 `npm run ops-report` 能看到本轮 `intake-email-orders: success`。
- 验证：`node --check` 覆盖 payment reply 库、CLI、intake、run_operations、ops-report、QA；手动 intake 测试把 `custom-order.txt` 解析为 `custom-niche`，生成 $49/month 付款回复包；默认空 inbox 路径稳定生成空管线；完整 `npm run operate` 成功采集 125 条、生成 20 份 outreach drafts、0 条 email intake、0 条 ready lead，末尾 `qa --skip-scheduler` 132/132 通过；完整 `npm run qa` 137/137 通过。
- 线上验证：提交 `a873b29 Add local email order intake workflow`，Pages run `27095892299` 成功；`npm run qa -- --online` 172/172 通过。Edge CDP 线上检查首页和 `/order/` 的桌面 1280x1100、移动 390x1100，均 `scrollWidth=clientWidth`、`clipped=false`；首页 12 cards、order/sample 链接存在，截图为 `dist/qa/online-intake-home-*-a873b29.png` 与 `dist/qa/online-intake-order-*-a873b29.png`。

## 2026-06-07 已付款邮件订单履约自动化

- 新增 `scripts/fulfill_email_orders.mjs` 与 `npm run fulfill-email-orders`：读取 `dist/email-order-intake/orders.json`，只为 stage 为 `paid_needs_fulfillment` 的本地邮件订单生成 `dist/orders/<order-id>/` 买家交付包；`payment_reply_drafted` 等未付款订单只进入 skipped 列表。
- 已接入 `npm run operate`：顺序为 `intake-email-orders` 之后、`fulfill-ready` 之前，因此 copied email order 如果包含 `Paid: yes`、`Payment confirmed` 或 `Stage: paid`，在外部确认付款后可自动准备本地履约目录。
- 安全边界：该脚本只生成本地 buyer delivery drafts，不发送邮件、不上传文件、不收款、不改 GitHub 状态；报告提醒发送前必须外部核实付款，且仍排除 `prospects.csv`、`outreach-board.md`、`latest.json`。
- 验证：手动创建 paid/unpaid 两封本地订单邮件，`npm run intake-email-orders` 解析 2 条，`npm run fulfill-email-orders` 准备 1 个 paid weekly order、跳过 1 个 unpaid sample order；生成目录只含 buyer deliverables、manifest、delivery email 和 checklist。`npm run qa` 143/143 通过；完整 `npm run operate` 成功，新增 `fulfill-email-orders: success`，末尾 `qa --skip-scheduler` 138/138 通过；手动 QA 残留订单目录已清理并重刷 ops report。
- 上线验证：提交 `6ff4a46 Add paid email order fulfillment workflow`，Pages run `27096347501` 成功；`npm run qa -- --online` 178/178 通过。Codex 内置浏览器线上检查首页和 `/order/` 的桌面 1280px、移动 390px，均 `scrollWidth=clientWidth`、无越界元素；首页和订单页各只有 1 个 H1，订单页 8 个 mailto CTA 正常。截图为 `dist/qa/online-email-fulfillment-*-viewport-6ff4a46.png`，full-page 备份为 `dist/qa/online-email-fulfillment-*-6ff4a46.png`。

## 2026-06-07 订单页复制草稿转化优化

- 新增 `/order/` copyable draft 按钮：每个套餐卡增加 `Copy draft / 复制草稿`，prepared email 区增加 `Copy English draft` 和 `Copy Chinese draft`，复用与 mailto 完全一致的订单草稿内容。
- 交互边界：复制按钮只在本地页面调用浏览器剪贴板；不发送邮件、不收集表单、不上传、不创建支付动作。若浏览器不给 clipboard 权限，会在当前卡片内插入并选中只读草稿文本框，买家仍可复制。
- QA 更新：`scripts/qa.mjs` 检查 `/order/` 至少 5 个 `data-copy-order`、`navigator.clipboard` 和 `copy-fallback`；线上 QA 也会检查 copyable drafts。顺手把 app result count 的 QA 断言同步到当前“Showing / 条机会”文案。
- 验证：`node --check scripts/build.mjs`、`node --check scripts/qa.mjs`、`npm run build`、`npm run qa` 144/144 通过。本地浏览器预览 `http://localhost:4191/order/`：桌面 1280px 与移动 390px 均 `scrollWidth=clientWidth`、5 个复制按钮、8 个 mailto、无越界元素；受限剪贴板点击后显示 `Draft selected` 并插入含 `Subject: TrendFoundry order` 的 fallback 文本框，移动点击态仍无横向溢出。截图在 `dist/qa/local-copy-order-*-v2.png` 和 `dist/qa/local-copy-order-mobile-clicked-v2.png`；临时 4191 服务已关闭。

## 2026-06-07 整站逻辑与视觉重构

- 首页信息架构已从“长销售说明优先”调整为“首屏产品承诺 + 当前 signal board 预览 + 免费样品 + 机会筛选器 + 定价/交付/SEO 复访入口”。用户进入后先看到真实产品状态，再决定查看样品或无登录下单。
- 新增 `scripts/generate_site_visuals.mjs` 与 `npm run visuals`，从 `data/latest.json` 生成 1200x760 `site/signal-board.png`。该图展示当前 issue、来源 lane、评分、high-fit 数量和前 8 个机会，不使用外部素材或版权不明图片。
- 首页和 `/zh/` 首屏已嵌入 `signal-board.png`，并加入轻量 CSS ticker 动画；移动端使用紧凑缩略图模式，隐藏重复 board summary，确保首屏底部露出下一段内容。
- 当前未提交 auth 路径已纳入 QA：`/auth/` 支持 Google、Apple、Microsoft、GitHub、WeChat、QQ、Alipay、DingTalk、Feishu 等 provider 展示；静态站只做配置/入口，不提交 secret，不完成 OAuth token exchange。
- QA 更新：`npm run qa` 现检查 `visuals` package script、首页/中文页 signal board、`site/signal-board.png` 尺寸、auth config 默认空 broker/provider disabled/no secret，以及 auth script 对未配置 provider 的阻断提示。
- 验证：`node --check scripts/build.mjs scripts/qa.mjs scripts/generate_site_visuals.mjs` 均通过；`npm run build`、`npm run visuals`、`npm run qa` 155/155 通过。Codex 内置浏览器本地预览 `http://localhost:4191/`：桌面 1280x720 与移动 390x844 均无横向溢出、无越界元素、两处 `signal-board.png` 均加载 1200x760；移动首屏高度从 1084px 压到 718px。最终截图在 `dist/qa/home-desktop-redesign-final.png`、`dist/qa/home-mobile-redesign-final.png`，检查 JSON 在 `dist/qa/visual-redesign-results-final.json`。
- 上线验证：提交 `66579c6 Redesign landing page with signal board visual`，Pages run `27097479725` 成功；`npm run qa -- --online` 197/197 通过。Codex 内置浏览器线上检查 `https://bravecownofear.github.io/trendfoundry/?v=66579c6`：桌面 1280x720 与移动 390x844 均无横向溢出、无越界元素、`signal-board.png` 加载完整；截图为 `dist/qa/online-home-desktop-redesign-66579c6.png` 和 `dist/qa/online-home-mobile-redesign-66579c6.png`。

## 2026-06-07 Signal board 日更自动化

- 修复视觉资产可能过期的问题：`package.json` 的 `daily` 现在在 `build` 后自动运行 `scripts/generate_site_visuals.mjs`，因此每日采集刷新后会同步重生 `site/signal-board.png`。
- 新增 `site/signal-board.meta.json`：记录 `dataGeneratedAt`、图片尺寸和前 8 个 top items，用于证明看板来自当前 `data/latest.json`，不是仅尺寸正确的旧图。
- QA 更新：`scripts/qa.mjs` 检查 `daily` 包含视觉生成、meta 的 `dataGeneratedAt` 等于当前数据、前 5 个 top items 的 URL/score 与当前 portfolio 选择一致；线上 QA 也检查 `signal-board.meta.json` 可访问且 data timestamp 匹配。
- 验证：`node --check scripts/generate_site_visuals.mjs scripts/qa.mjs` 通过；`npm run daily` 成功采集 125 条并重生 `signal-board.png` / `signal-board.meta.json`；`npm run commerce && npm run launch-assets && npm run ops-report && npm run qa` 158/158 通过。当前 meta 对应 `dataGeneratedAt=2026-06-07T16:03:03.911Z`，Top #8 已更新为 `BV1KnEt6MECH`，本地看板视觉检查正常。
- 上线验证：提交 `47041d7 Keep signal board fresh in daily runs`，Pages run `27097689285` 成功；`npm run qa -- --online` 202/202 通过，包含线上 `signal-board.png` 1200x760、`signal-board.meta.json` HTTP 200 和 data timestamp 匹配检查。

## 2026-06-07 Free sample pack 下载包

- 新增 `scripts/export_public_sample_download.mjs` 与 `npm run free-pack`：把公开样品、双语 CSV、`ready-to-record-script.md`、`signal-board.png`、`signal-board.meta.json` 和订单说明打包成 `site/trendfoundry-free-sample-pack.zip`，同时生成 `site/trendfoundry-free-sample-pack.json` manifest。
- 已接入 `npm run daily`：每日采集、构建、视觉看板刷新后，会自动生成最新可下载样品包；该包只包含公开 proof assets，不包含 `prospects.csv`、`outreach-board.md`、`latest.json`、本地线索或私密订单数据。
- 转化入口：首页首屏 utility link 已把低价值 `CSV` 替换成 `Sample pack / 样品包`；首页样品区和 `/zh/` 样品区增加 `Download sample pack / 下载样品包`；`/order/` hero 增加 `Download sample pack`，让买家下单前一键下载完整免费样品包。
- QA 更新：`scripts/qa.mjs` 检查 `free-pack` package script、`daily` 包含下载包生成、首页/中文页/订单页/sitemap 链接 ZIP、ZIP 内包含公开文件并排除 seller-only 文件；线上 QA 也下载 ZIP 并解析中央目录确认文件清单。
- 顺手修复 `scripts/collect.mjs` 的 hook 质量退化：恢复为带具体 `item.title` 的“复现步骤、限制和创作价值”文案，避免样品包每条机会都变成泛泛的“验证来源、复现步骤和限制”。
- 验证：`node --check scripts/export_public_sample_download.mjs scripts/build.mjs scripts/qa.mjs` 通过；`npm run build && npm run visuals && npm run free-pack && npm run qa` 167/167 通过。本地 Codex 内置浏览器检查 390px 移动端：首页首屏显示 `Sample pack` 链接，订单页显示 `Download sample pack`，两页均无横向溢出；截图在 `dist/qa/local-free-pack-home-mobile-final.png` 和 `dist/qa/local-free-pack-order-mobile.png`。最终重跑 `npm run daily && npm run commerce && npm run launch-assets && npm run ops-report && npm run qa` 成功，日更链路自动生成 ZIP、看板和导出包，QA 167/167 通过。
- 上线验证：提交 `72848db Add downloadable public sample pack`，Pages run `27098024358` 成功；`npm run qa -- --online` 218/218 通过，线上 ZIP HTTP 200、manifest 可解析、公开文件齐全且 seller-only 文件缺席。Codex 内置浏览器线上检查移动 390x844：首页首屏 `Sample pack` 可见、`signal-board.png` 加载且进入首屏，订单页首屏 `Download sample pack` 与 `GitHub request` 可见，两页 `scrollWidth=clientWidth=375`、无横向溢出；Edge headless 截图为 `dist/qa/online-free-pack-home-mobile-edge-72848db.png` 和 `dist/qa/online-free-pack-order-mobile-edge-72848db.png`。

## 2026-06-07 Animated workflow proof 收尾

- 新增 `site/signal-demo.svg`：由 `scripts/generate_site_visuals.mjs` 从当前 `data/latest.json` 自动生成，展示 public signals -> ranked ideas -> scripts/sample pack -> order/delivery 的动态工作流；不依赖外部图片素材或版权不明资产。
- 首页和 `/zh/` 已把产品证明顺序调整为：offer -> free sample -> static signal board preview -> animated workflow preview -> opportunity finder -> grid -> pricing/delivery。这样访客先理解产品和交付逻辑，再进入长机会列表。
- `signal-demo.svg` 已写入 `sitemap.xml`，加入 `trendfoundry-free-sample-pack.zip` 与 manifest，成为可下载公开 proof asset；ZIP 仍排除 `prospects.csv`、`outreach-board.md`、`latest.json` 和私密线索/订单数据。
- QA 更新：`scripts/qa.mjs` 检查首页/中文页引用 `signal-demo.svg`、SVG 含 `@keyframes` / `demo-row` / workflow 文案、sitemap 包含动画、免费 ZIP 包含动画；线上 QA 也会验证 SVG HTTP 200 和动画结构。
- 视觉修复：取消 `reveal-item` 初始透明度，避免 headless/慢设备首屏淡白；修复 hero metrics 的嵌套 span 样式；移动端 H1 上限降到 42px，避免英文标题裁切。
- 本地验证：`npm run build && npm run visuals && npm run free-pack && npm run qa` 171/171 通过。Codex Browser 390x844 审计：首页和 `/zh/` 均 `scrollWidth=clientWidth=375`、无越界元素；动画区渲染尺寸约 335x189，位于样品/看板之后、机会筛选器之前；英文 H1 宽 335px、右边界 355px。截图为 `dist/qa/local-motion-home-mobile-final5.png`，中途缺陷截图保留在 `dist/qa/local-motion-home-mobile-final*.png` 便于回看。
- 上线验证：提交 `32ca33b Add animated workflow proof`，Pages run `27098807147` 成功；`npm run qa -- --online` 225/225 通过。Codex Browser 线上 390x844 审计 `https://bravecownofear.github.io/trendfoundry/?v=32ca33b&lang=en`：`scrollWidth=clientWidth=375`、无越界元素、英文 H1 宽 335px 右边界 355px、`signal-demo.svg` 存在且渲染 335x189、首屏 `Sample pack` 入口可见。

## 2026-06-07 内容侧审计工作台

- 用户已安排另一个 UI 设计师 agent 继续前端设计；本 agent 后续主责转向内容侧：选题质量、脚本、样品包、外联素材、交付物和运营流水线。
- 新增 `scripts/generate_content_audit.mjs` 与 `npm run content-audit`：读取 `data/latest.json` 的当前 12 条组合，按 evidence、recordability、specificity、commercial handoff、risk 五个维度给内容分数，并生成 `docs/content-editorial-audit.md` 与 ignored `dist/content-audit/latest.json`。
- 当前审计结论：12 条 portfolio 中 11 条 high-fit，0 条 weak demo，12 条都存在 generic why-now，4 条存在 generic hook。说明素材池和录制结构基本可用，但样品包/脚本仍需要把“为什么现在讲”改成具体 release、更新、benchmark、争议或采用信号。
- 下一步内容优先级：先重写 top 6 episode briefs 的 opening hook 和 why-now，再为每条 paid-pack candidate 指定一个可录屏的 terminal/browser proof；每次内容改动后运行 `npm run content-audit`，观察 generic hook / generic why-now 数量是否下降。
- 并行注意：当前工作树存在 UI 设计师 agent 的前端改动（`scripts/build.mjs`、`site/app.js`、`site/index.html`、`site/styles.css`、`site/zh/index.html`）；内容提交不要混入这些文件，避免覆盖前端协作进度。
- 分工更新：用户已安排另一个 UI 设计师 agent 继续前端设计；本版本收尾后，本项目负责人后续主抓内容侧，包括选题质量、脚本、样品包、外联素材、交付物和运营流水线，不再主动扩展前端视觉方向。

## 2026-06-07 内容生成质量升级

- 升级 `scripts/collect.mjs`：`whyNow()` 不再输出 `current trend signal / timely candidate` 模板，而是按来源写入可观察证据。GitHub 使用 pushed freshness、stars、open issues；Bilibili/YouTube 使用发布日期、播放/浏览、作者/摘要；HN 使用 points/comments；arXiv 使用发布时间和摘要。
- 新增内容 topic 提炼：长视频标题会归一化为 `ComfyUI AI 视频工作流`、`AI 视频工具栈`、`AI 视频提示词生成器` 等买家可读主题，避免 B 站超长标题在 hook 中被截成半句。
- 升级 `demoSteps()`：GitHub 条目要求 capture README promise、latest pushed date、stars/issues、quickstart command、input/output 和 adoption blockers；视频条目要求截取原视频承诺并做 tiny input 复现；HN/arXiv 分别变成争议聚类和论文小实验。
- 升级 `scripts/generate_content_audit.mjs`：当 rewrite queue 为空时，Next Content Workbench 自动切换为 ready-to-record 队列动作，而不是继续提示重写 generic hook。
- 验证：`node --check scripts\collect.mjs`、`node --check scripts\generate_content_audit.mjs`、`npm run collect`、`npm run content-audit` 均通过；最新 `docs/content-editorial-audit.md` 显示 Portfolio 12、High-fit 11、Generic hooks 0、Generic why-now notes 0、Weak demos 0、Rewrite Queue 为空。
- 内容门禁：Python 检查确认 `data/latest.json` 和 `docs/content-editorial-audit.md` 无 replacement char / 问号乱码；旧 generic hook / why-now 模板为 0；ComfyUI、AI 视频工具栈、AI 视频提示词生成器三个归一化 topic 均出现在 hook 中。
- 并行边界：本轮没有运行会覆盖 `site/` 的 `npm run build`，也不提交 UI 设计师 agent 的 `scripts/build.mjs` 与 `site/` 改动。

## 2026-06-07 Episode Workbench

- 新增 `scripts/generate_episode_workbench.mjs` 与 `npm run episode-workbench`：从当前 high-fit normal-quality portfolio 中选 5 条 ready items，生成 `docs/episode-workbench.md` 和 ignored `dist/episode-workbench/latest.json`。
- 输出内容：每条 episode 包含 Bilibili opening、YouTube opening、Recording Proof、Buyer Handoff；proof asset 明确到 terminal capture、browser capture、comment cluster 或 paper/toy demo，不只停留在选题标题。
- 当前 5 条 ready queue：NousResearch/hermes-agent、infiniflow/ragflow、google-gemini/gemini-cli、langchain-ai/langchain、The Only AI Video Prompt Generator You'll Ever Need。
- 验证：`node --check scripts\generate_episode_workbench.mjs` 与 `npm run episode-workbench` 通过；Python 门禁确认文档含 Bilibili/YouTube opening、Recording Proof、Buyer Handoff，5 条 episode 完整，无 replacement char、问号乱码、旧 generic why-now、中文标题混入英文 `is only/worth` 句式或双句号。
- 下一步内容动作：把 `docs/episode-workbench.md` 中排名第一的 GitHub proof episode 扩展为真正 6-8 分钟成片脚本，或用第 5 条 YouTube prompt generator 作为更贴近视频创作者的公开样品。

## 2026-06-07 Full Episode Script

- 新增 `scripts/generate_full_episode_script.mjs` 与 `npm run full-script`：从当前最高分 high-fit normal 条目生成完整 6-8 分钟 proof-first 成片脚本，输出 `docs/full-episode-script.md` 和 ignored `dist/full-episode-script/latest.json`。
- 当前生成对象：`NousResearch/hermes-agent`。脚本包含 Script Contract、Title Options、70-Second Cold Open、Scene-By-Scene Script、Recording Checklist、Buyer Delivery Notes、Shorts / Clip Hooks 和 Publishing Metadata。
- 内容定位：这份脚本比 `episode-workbench` 更接近买家可用交付物；它不只给标题和开场，还明确每段屏幕内容、旁白、用途、录制 proof asset、失败/摩擦备注和 CTA。
- 验证：`node --check scripts\generate_full_episode_script.mjs` 与 `npm run full-script` 通过；Python 门禁确认至少 8 个 scenes、7 条以上 recording checklist、含 TrendFoundry sample pack CTA、无 replacement char、问号乱码、旧 generic why-now、收益/播放保证承诺或中英句号叠加。
- 并行边界：没有运行 `npm run build`，没有覆盖或提交 UI 设计师 agent 的 `scripts/build.mjs` 与 `site/` 改动；本轮只新增内容侧交付生成器和文档。

## 2026-06-07 Buyer Content Pack

- 新增 `scripts/generate_buyer_content_pack.mjs` 与 `npm run buyer-pack`：把 `full-episode-script.md`、`episode-workbench.md`、`content-editorial-audit.md` 包装成 buyer-facing 交付索引，输出 `docs/buyer-content-pack.md` 和 ignored `dist/buyer-content-pack/`。
- `dist/buyer-content-pack/` 生成 `README.md`、`delivery-email.md`、`manifest.json` 以及三份买家内容文件副本；用于人工审核后直接发给样品包或付费买家。
- 交付边界：manifest 明确 buyer deliverables 只有 `full-episode-script.md`、`episode-workbench.md`、`content-editorial-audit.md`；排除 `prospects.csv`、`outreach-board.md`、`data/latest.json`、raw snapshots、lead pipeline、sensitive payment data 和 account data。
- 验证：`node --check scripts\generate_buyer_content_pack.mjs` 与 `npm run buyer-pack` 通过；Python 门禁确认 6 个 dist 文件齐全、文档含 Included Deliverables / Delivery Boundary / Suggested Delivery Email / Operator Checklist、manifest buyer deliverables 精确匹配、未包含 seller-only 文件、无 replacement char、问号乱码、收益/播放保证承诺、支付凭证请求或双句号。
- 并行边界：没有运行 `npm run build`，没有提交 `site/`、`scripts/build.mjs` 或其他 UI agent 生成文件。

## 2026-06-07 Content Ops Lane

- 新增 `scripts/run_content_ops.mjs` 与 `npm run content-ops`：默认串联 `content-audit`、`episode-workbench`、`full-script`、`buyer-pack`、`custom-proof-pack`、`content-listing`、`content-subscription`、`content-sales`、`content-prospects`、`content-crm`、`content-revenue`、`content-feedback`、`content-close`、`content-health`，输出 `docs/content-ops.md` 和 ignored `dist/content-ops/latest-run.json`。
- 默认行为：不联网刷新公开来源、不发送消息、不收款、不运行 `npm run build`、不覆盖前端；如需要刷新来源，可用 `npm run content-ops -- --refresh` 显式先跑 `collect`。
- 当前验证结果：`npm run content-ops` 成功，步骤为 content-audit / episode-workbench / full-script / buyer-pack / custom-proof-pack / content-listing / content-subscription / content-sales / content-prospects / content-crm / content-revenue / content-feedback / content-close / content-health，全部 exit 0；`docs/content-ops.md` 显示 Total source items、Source errors、Primary episode、Custom pack、Subscription plan、Sales drafts、Prospects、CRM、Revenue model、Feedback loop、Close pack、Health gate 和 Listing SKUs。
- 安全门禁：Python 检查确认 run JSON 的 safety 为 sendsMessages=false、collectsPayment=false、buildsFrontend=false、includesSellerOnlyFiles=false；buyer deliverables 只含三份内容文件；无 replacement char、问号乱码、收益/播放保证承诺、密码或钱包种子请求。
- 用途：这是内容侧日常运营入口。UI agent 并行设计时，本 agent 可以只跑 `npm run content-ops` 刷新内容交付包，而不改动 `site/`。

## 2026-06-07 Content Product Listing

- 新增 `scripts/generate_content_listing.mjs` 与 `npm run content-listing`：读取当前 buyer content pack manifest，生成 `docs/content-product-listing.md` 和 ignored `dist/content-listing/`。
- 输出内容：`products.json`、`products.csv`、`platform-listings.md`、`manual-invoices.md`，用于 Gumroad、Ko-fi、Lemon Squeezy、Stripe Payment Links、Buy Me a Coffee 或手动 invoice 上架/复制。
- 三个 SKU：`trendfoundry-proof-script-pack`（USD 9 one-time）、`trendfoundry-proof-weekly`（USD 19/month）、`trendfoundry-proof-custom`（USD 49/month）。前两档履约普通内容三件套；custom 档履约 `custom-proof-pack.md`，可选附加普通内容三件套。
- 安全边界：listing 明确 external payment confirmation required，不请求 sensitive payment/account data；不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
- 验证：`node --check scripts\generate_content_listing.mjs` 与 `npm run content-listing` 通过；Python 门禁确认 3 个产品、3 行 CSV、4 个 dist 文件、SKU 精确匹配、fulfillment 不含 seller-only 文件、无 replacement char、问号乱码、guaranteed views/revenue 等高风险短语。
- 并行边界：没有运行 `npm run build`，没有提交 `site/` 或 `scripts/build.mjs`。

## 2026-06-07 Content Ops Listing 集成

- `scripts/run_content_ops.mjs` 已把 `custom-proof-pack`、`content-listing`、`content-sales`、`content-prospects` 与 `content-crm` 加入默认流水线；现在 `npm run content-ops` 顺序为 `content-audit` -> `episode-workbench` -> `full-script` -> `buyer-pack` -> `custom-proof-pack` -> `content-listing` -> `content-sales` -> `content-prospects` -> `content-crm`。
- `docs/content-ops.md` 现在显示 listing SKUs：`trendfoundry-proof-script-pack`、`trendfoundry-proof-weekly`、`trendfoundry-proof-custom`，并在 Next Operator Action 中提醒先审核 `docs/content-product-listing.md` 再发布或复制支付平台字段。
- 验证：`node --check scripts\run_content_ops.mjs`、`npm run content-ops` 和 Python 门禁通过；run JSON 九步全部 success，safety 仍为 sendsMessages=false、collectsPayment=false、buildsFrontend=false、includesSellerOnlyFiles=false。
- 并行边界：仍未运行 `npm run build`，未提交 `site/` 或 `scripts/build.mjs`。

## 2026-06-07 Content Pack Fulfillment

- 新增 `scripts/fulfill_content_order.mjs` 与 `npm run fulfill-content`：读取 `dist/buyer-content-pack/manifest.json`，为已外部确认付款的内容包买家准备本地 `dist/content-orders/<order-id>/`。
- 买家交付物固定为 `full-episode-script.md`、`episode-workbench.md`、`content-editorial-audit.md`；生成的 `manifest.json`、`delivery-email.md` 和 `fulfillment-checklist.md` 是本地操作辅助。
- `--payment-ref` 默认 `not-provided`，只有传入外部确认引用时 manifest 才标记 `paymentExternallyConfirmed=true`；命令不收款、不发邮件、不上传、不请求密码、卡号、私钥或钱包种子。
- 文档入口：`docs/content-fulfillment.md`。

## 2026-06-07 Weekly Subscription Fulfillment

- 新增 `scripts/fulfill_content_subscription.mjs` 与 `npm run fulfill-content-subscription`：读取 ignored `dist/content-subscription-plan/calendar.csv` 和 `manifest.json`，按 `--subscriber`、`--contact`、`--channel`、`--week`、`--payment-ref` 生成本地 `dist/content-subscriptions/<order-id>/`。
- 买家交付物为 `weekly-proof-pack.md` 和 `subscriber-email.md`；本地操作文件为 `backup-candidates.csv`、`manifest.json` 和 `fulfillment-checklist.md`。
- 文档入口：`docs/content-subscription-fulfillment.md`；该命令只用于外部确认订阅/付款后的手动交付准备，不发送消息、不收款、不上传、不请求 sensitive payment/account data，不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
- QA 验证：`npm run fulfill-content-subscription -- --subscriber="QA Subscriber" --contact="qa@example.com" --channel="https://youtube.com/@qa" --week="2" --payment-ref="qa-external-confirmation" --order-id="qa-weekly-subscription"` 成功；输出 5 个预期文件，manifest productSku 为 `trendfoundry-proof-weekly`、week 为 2、paymentExternallyConfirmed=true。

## 2026-06-08 Content Subscription CRM

- 新增 `scripts/generate_content_subscription_crm.mjs` 与 `npm run content-subscription-crm`：读取 ignored `data/content-subscriptions/subscribers.csv`，按 weekly plan 判断 `prepare_delivery`、`payment_review_before_delivery`、`renewal_check`、`upcoming` 和 `inactive`。
- 输出公开 `docs/content-subscription-crm.md`，只写计数、流程和安全边界；真实 subscriber queue、命令清单和 CSV 留在 ignored `dist/content-subscription-crm/`。
- `dist/content-subscription-crm/subscribers.template.csv` 提供私密 subscriber CSV schema；`.gitignore` 已加入 `data/content-subscriptions/`。
- `content-ops` 已接入 `content-subscription-crm`，现在默认 15 步，并在 `docs/content-ops.md` 显示 subscription CRM 计数。
- 验证：`node --check`、`npm run content-subscription-crm`、`npm run content-ops` 和 Python 结构化门禁通过；当前 subscriber rows 为 0、ready today 0、payment review 0，health gate checkedFileCount 为 22 且 mojibake/leak 均为 0。

## 2026-06-08 Content Subscription Delivery Completion

- 新增 `scripts/complete_content_subscription_delivery.mjs` 与 `npm run complete-content-subscription-delivery`：读取 ignored `dist/content-subscriptions/<order-id>/manifest.json`，在周包人工审核并发送后回写 ignored `data/content-subscriptions/subscribers.csv`。
- 回写字段：`last_delivered_week`、`last_sent_at`、`next_delivery_date`，并保留 subscriber contact、channel、payment_ref、paid_through 与 notes。
- `docs/content-subscription-fulfillment.md` 已把订阅 SOP 串成：`content-subscription` -> `record-content-subscription` -> `content-subscription-crm` -> `fulfill-content-subscription` -> `complete-content-subscription-delivery` -> `content-subscription-crm`。
- QA 验证：临时 subscriber `qa@example.com` 走完整链路后，week 1 交付完成会把下一次交付日写成当前 plan 的 week 2 日期 `2026-06-24`；随后测试 subscriber 和测试订单目录已删除，CRM 恢复 0 rows。
- 最终验证：`node --check`、完整 `npm run content-ops` 和 Python 门禁通过；content ops 仍为 15 步，health gate checkedFileCount 为 22，mojibake/leak 均为 0。

## 2026-06-08 Content Subscription Due Batch

- 新增 `scripts/fulfill_due_content_subscriptions.mjs` 与 `npm run content-subscription-due`：读取 ignored `dist/content-subscription-crm/due-queue.csv`，只对 `action=prepare_delivery` 的 subscriber 行批量生成 buyer-only weekly pack。
- 输出公开 `docs/content-subscription-due.md`，只包含 queue/prepared/failed 计数和 SOP；私密准备明细留在 ignored `dist/content-subscription-due/manifest.json`、`prepared.csv`、`prepared.md`。
- `content-ops` 已接入 `content-subscription-due`，现在默认 16 步；`content-health` 已把公开 due doc 和 ignored prepared outputs 纳入文本健康检查。
- QA 验证：临时 subscriber `due@example.com` 由 `record-content-subscription` 写入、`content-subscription-crm` 标记 ready、`content-subscription-due` 自动生成 `dist/content-subscriptions/2026-06-08-due-example-com-week-1/`，公开文档不含邮箱；随后测试 subscriber 和测试订单目录已删除，due prepared 恢复 0。
- 最终验证：`node --check`、完整 `npm run content-ops` 和 Python 门禁通过；content ops 为 16 步，health gate checkedFileCount 为 25，mojibake/leak 均为 0。

## 2026-06-08 Content Subscription Retention

- 新增 `scripts/generate_content_subscription_retention.mjs` 与 `npm run content-subscription-retention`：读取 ignored `dist/content-subscription-crm/due-queue.csv`，为 `payment_review_before_delivery` 与 `renewal_check` 行生成私密待审核草稿。
- 输出公开 `docs/content-subscription-retention.md`，只包含 queue/draft/payment-review/renewal 计数和 SOP；私密草稿留在 ignored `dist/content-subscription-retention/drafts.md`、`drafts.csv` 和 `manifest.json`。
- `content-ops` 已接入 `content-subscription-retention`，现在默认 17 步；`content-health` 已把公开 retention doc 和 ignored retention drafts 纳入文本健康检查。
- QA 验证：临时 subscriber `pay@example.com` 触发 payment review，`renew@example.com` 触发 renewal check；私密草稿含联系人和安全边界，公开文档不含邮箱；随后测试 subscriber 已删除，retention drafts 恢复 0。
- 最终验证：`node --check`、完整 `npm run content-ops` 和 Python 门禁通过；content ops 为 17 步，health gate checkedFileCount 为 28，mojibake/leak 均为 0。

## 2026-06-08 Email Subscription Sync

- 新增 `scripts/sync_email_subscriptions.mjs` 与 `npm run sync-email-subscriptions`：读取 `dist/email-order-intake/orders.json`，只把 `stage=paid_needs_fulfillment` 且 `tier=weekly-brief` 的 email order 写入 ignored `data/content-subscriptions/subscribers.csv`。
- `scripts/run_operations.mjs` 已在 `intake-email-orders` 后、`fulfill-email-orders` 前接入 `sync-email-subscriptions`、`content-subscription`、`content-subscription-crm`、`content-subscription-due` 和 `content-subscription-retention`，让已付款 weekly email order 自动进入 subscription CRM 并准备首期周包。
- 输出公开 `docs/email-subscription-sync.md`，只包含订单/同步/跳过计数和 SOP；同步明细留在 ignored `dist/email-subscription-sync/manifest.json` 与 `synced.csv`。
- 空同步行为已修正：当没有 paid weekly order 且不存在已有 subscriber rows 时，不再创建只有表头的 `data/content-subscriptions/subscribers.csv`。
- QA 验证：临时 `qa-weekly-paid.txt` email order 被 intake 识别为 paid weekly，sync 写入 `weekly@example.com` 到 private subscriber CSV，subscription CRM 读到 1 row；公开 sync doc 不含邮箱。随后临时 email、subscriber CSV 已删除，空同步恢复 syncedCount 0。
- 首期交付修正：paid weekly email order 的 `next_delivery_date` 默认写为当天（可用 `--first-delivery-date` 覆盖），匹配付款回复里的 first issue within 24 hours 承诺。
- 首期交付 QA：临时 `qa-weekly-immediate.txt` paid weekly order 走 `intake-email-orders` -> `sync-email-subscriptions` -> `content-subscription` -> `content-subscription-crm` -> `content-subscription-due` 后，CRM ready today 为 1，due batch 生成 `dist/content-subscriptions/2026-06-08-immediate-example-com-week-1/`；公开 sync doc 未泄露 `immediate@example.com`。随后临时 email、subscriber CSV 和测试订单目录已删除，health gate checkedFileCount 30、mojibake/leak 均为 0。
- 重复履约修正：`scripts/fulfill_email_orders.mjs` 已跳过 paid `weekly-brief`，避免同一 weekly 付款同时生成普通 `dist/orders/` 订单和 subscription 周包。QA 验证：临时 `qa-weekly-no-duplicate.txt` 只生成 `dist/content-subscriptions/2026-06-08-nodup-example-com-week-1/` 周包，普通 `dist/orders/email-qa-weekly-no-duplicate-weekly-brief/` 不存在；`dist/email-fulfillment/email-orders.json` 记录 skipped reason。

## 2026-06-08 Custom Email Fulfillment

- 新增 `scripts/fulfill_custom_email_orders.mjs` 与 `npm run fulfill-custom-email-orders`：读取 `dist/email-order-intake/orders.json`，只处理 `stage=paid_needs_fulfillment` 且 `tier=custom-niche` 的订单。
- 专用输出：ignored `dist/custom-email-orders/<order-id>/`，包含 `custom-proof-pack.md`、`delivery-email.md`、`manifest.json` 和 `fulfillment-checklist.md`；公开 `docs/custom-email-fulfillment.md` 只显示私密占位与计数。
- `scripts/run_operations.mjs` 已在 subscription due/retention 后、普通 `fulfill-email-orders` 前接入 `fulfill-custom-email-orders`。
- `scripts/fulfill_email_orders.mjs` 已跳过 paid `custom-niche`，避免同一 custom 付款同时生成通用 `dist/orders/` 样品包和 custom proof pack。
- QA 验证：临时 `qa-custom-paid.txt` 生成 custom order，`dist/custom-email-orders/email-qa-custom-paid-custom-niche/` 含 custom proof pack，普通 `dist/orders/email-qa-custom-paid-custom-niche/` 不存在；JSON skipped reason 为 `custom_order_handled_by_fulfill_custom_email_orders`。公开文档不含 `custom@example.com`、买家名或频道；测试 email 与订单目录已清理，health gate checkedFileCount 32、mojibake/leak 均为 0。

## 2026-06-08 Email Order Routing Audit

- 新增 `scripts/audit_email_order_routing.mjs` 与 `npm run audit-email-order-routing`：读取 `dist/email-order-intake/orders.json`，把每个 email order 归到 `payment_reply_only`、`standard_sample_pack`、`weekly_subscription` 或 `custom_proof_pack`。
- `scripts/run_operations.mjs` 已在 `intake-email-orders` 后接入 routing audit；`content-health` 已覆盖公开 routing doc 和 ignored routes CSV。
- QA 验证：4 封临时邮件覆盖 paid sample、paid weekly、paid custom 和 unpaid sample；routing counts 为 standard=1、weekly=1、custom=1、paymentReply=1；公开 doc 不含任何测试邮箱。测试邮件已删除，空 intake 后 routing total 恢复 0，health gate checkedFileCount 34、mojibake/leak 均为 0。

## 2026-06-08 Fulfillment Idempotency

- `scripts/fulfill_email_orders.mjs`、`scripts/fulfill_custom_email_orders.mjs` 和 `scripts/fulfill_due_content_subscriptions.mjs` 现在都会先检查目标订单目录的 `manifest.json`；若交付包已存在，则报告 `already_prepared`，不再覆盖 buyer-only 目录。
- 普通 email 履约继续排除 `weekly-brief` 与 `custom-niche`：weekly 交给 subscription due workflow，custom 交给 custom proof pack workflow；JSON/Markdown 报告都保留 status 或 skip reason。
- 控制台输出已拆分为新生成数量和已存在数量，方便计划任务日志判断重复运行是否安全。
- QA 验证：临时 sample email 第一次生成 `prepared`、第二次为 `already_prepared`；临时 custom email 第一次由 custom handler 生成、第二次为 `already_prepared`；临时 weekly/custom 在普通 email fulfillment 中只记录专用 skip reason，未生成通用订单目录。
- 清理验证：临时 email、测试订单目录均已删除；最终 `npm run content-health` 通过，`filesWithMojibakeMarkers=0`、`publicCloseDocProspectLeaks=0`，公开/报告文件不含测试邮箱。

## 2026-06-08 Content Outreach Review

- 新增 `scripts/generate_content_outreach_review.mjs` 与 `npm run content-outreach-review`：读取 ignored `dist/content-close-pack/today-close-queue.csv` 和产品 listing，为每日 close queue 生成 reviewer-ready send packs。
- 私密输出：`dist/content-outreach-review/review-board.md`、`review-board.csv`、`manifest.json` 和 `send-packs/*.md`；每个 send pack 包含 subject、正文、offer、proof URL、follow-up date、人工审核 checklist 和 reply capture row。
- 公开输出：`docs/content-outreach-review.md` 只显示 review pack 数、offer mix、操作流程和安全边界，不暴露 creator 名或 proof URL。
- `content-ops` 已接入该步骤，当前内容侧流水线为 18 步；`content-health` 已覆盖公开说明与私密 review board。
- QA 验证：`npm run content-outreach-review` 生成 5 个 send packs；完整 `npm run content-ops` 成功，run JSON 显示 `reviewPackCount=5`；`npm run content-health` 检查 37 个文件，mojibake/leak 均为 0；额外 Node 检查确认公开 docs 不含私密 close queue 的 creator 或 proof URL。

## 2026-06-08 Content Deal Desk

- 新增 `scripts/generate_content_deal_desk.mjs` 与 `npm run content-deal-desk`：读取 ignored `data/content-sales-crm/replies.csv`、CRM pipeline 和产品 listing，把真实回复或 `paid_needs_fulfillment` 阶段转成成交处理台。
- 私密输出：`dist/content-deal-desk/deal-desk.md`、`deal-desk.csv`、`response-drafts.md`、`invoice-drafts.md`、`fulfillment-commands.ps1`、`reply-template.csv` 和 `objection-playbook.csv`。
- 无回复时仍生成 4 条 objection playbook：price sensitive、narrower niche、recurring help、more proof；有回复时生成对应 reply draft、invoice draft 和 external-payment-confirmed 后的履约命令。
- 公开输出：`docs/content-deal-desk.md` 只显示 active deal 数、playbook 数、action mix、流程和安全边界，不暴露 buyer/prospect 行。
- `content-ops` 已接入该步骤，当前内容侧流水线为 19 步；`content-health` 已覆盖公开说明和私密 deal desk 输出。
- QA 验证：无回复路径 `activeDealCount=0`、`playbookCount=4`；临时 reply 模拟 invoice/custom 购买意向时生成 `verify_external_payment_then_fulfill`、invoice draft 和 `npm run custom-proof-pack` command；临时 reply 已删除。最终 `npm run content-ops` 成功，health gate 检查 42 个文件，mojibake/leak 均为 0，公开 docs 不含模拟 creator/proof URL。

## 2026-06-08 Operate Content Lane Integration

- `scripts/run_operations.mjs` 已在 `daily` 后、`commerce` 前接入 `content-ops`，使 Windows 计划任务、GitHub Actions 和本地 `npm run operate` 都会刷新内容审计、买家包、listing、订阅队列、prospecting、CRM、收入模型、反馈、close queue、outreach review、deal desk 和 health gate。
- `scripts/report_ops.mjs` 已新增 `Content Sales Lane`：汇总 content ops status、19 步内容流水线、primary episode、buyer deliverables、prospects、CRM due today、close queue、outreach review packs、deal desk active deals/playbook rows 和 health gate 计数。
- 验证：`node --check scripts\run_operations.mjs`、`node --check scripts\report_ops.mjs`、`npm run content-ops`、`npm run ops-report` 和结构化 Node 门禁通过；当前 `contentStatus=success`、`contentSteps=19`、`activeDeals=0`。
- 安全边界：该集成只刷新本地内容与运营报告，不发送消息、不收款、不上传文件、不 build 前端；前端/UI 并行改动未纳入本次提交。

## 2026-06-08 Content Evidence Pack

- 新增 `scripts/generate_content_evidence_pack.mjs` 与 `npm run content-evidence`：读取 `dist/full-episode-script/latest.json`、`dist/episode-workbench/latest.json` 和 `data/latest.json`，生成买家可用的事实核查/证据包。
- 输出：公开 `docs/content-evidence-pack.md`；ignored `dist/content-evidence-pack/content-evidence-pack.md`、`evidence.csv`、`claim-checklist.csv`、`manifest.json`。每条 claim 包含 source URL、timeliness、recordable proof、limitation、verification step 和 buyer use。
- `buyer-pack` 已升级为四件套：`full-episode-script.md`、`episode-workbench.md`、`content-evidence-pack.md`、`content-editorial-audit.md`；`fulfill-content` 会按 manifest 自动复制四份买家交付物。
- `content-ops` 已在 `full-script` 后、`buyer-pack` 前接入 `content-evidence`，当前内容侧流水线为 20 步；`content-health` 覆盖 evidence doc、dist evidence CSV、full/episode JSON 和关键内容脚本源码。
- 内容质量修复：`generate_full_episode_script.mjs` 与 `generate_episode_workbench.mjs` 的叙述模板已改为 ASCII-only 英文，避免可卖脚本里再次出现源代码层面的中文 mojibake；Bilibili 标题/数据源中文仍保留并由 UTF-8 health gate 判断。
- 验证：语法检查、完整 `npm run content-ops`、`npm run ops-report` 和结构化 Node 门禁通过；当前 content steps 20、buyer deliverables 4、evidence items 5、claims 20、health checked files 53、mojibake/leak 均为 0。

## 2026-06-08 Content Fulfillment Idempotency

- `scripts/fulfill_content_order.mjs` 现在会先检查 `dist/content-orders/<order-id>/manifest.json`；如果订单已存在，输出 `already_prepared` 并退出，不覆盖四件套 buyer deliverables、delivery email 或 checklist。
- 默认 buyer deliverables fallback 已同步为四件套：`full-episode-script.md`、`episode-workbench.md`、`content-evidence-pack.md`、`content-editorial-audit.md`。
- 验证：临时 `qa-content-idempotent` 第一次生成内容订单，第二次返回 `already_prepared`；订单 manifest 保留 4 个 buyer deliverables，未触碰前端/UI 文件。

## 2026-06-08 Content Sale Status Recorder

- 新增 `scripts/update_content_sale_status.mjs` 与 `npm run record-content-sale`：按 `source + creator` upsert ignored `data/content-sales-crm/overrides.csv`，可选同步 reply 摘要到 ignored `replies.csv`。
- 支持阶段：`draft_review_before_sending`、`sent_waiting_reply`、`replied_needs_response`、`qualified_needs_custom_pack`、`paid_needs_fulfillment`、`fulfilled_waiting_feedback`、`closed_won`、`not_fit`。
- `content-sales-crm` 和 `content-deal-desk` 公开说明已加入该命令，鼓励用命令回写 sent/replied/paid/fulfilled 状态，不再手工改 CSV。
- QA 验证：临时写入 YouTube prospect `replied_needs_response` + private reply 后，`content-crm` 读到 due item，`content-deal-desk` 生成 1 条 active deal；公开 docs 不泄露真实 creator、QA notes、summary 或 objection。测试后 private CSV 已恢复/删除，deal desk 恢复 active deals 0。
- `content-health` 覆盖 `scripts/update_content_sale_status.mjs`，当前 checked files 54、mojibake/leak 均为 0。

## 2026-06-08 Ops Report Evidence Metrics

- `scripts/report_ops.mjs` 的 Content Sales Lane 已补充 evidence pack 指标：`evidenceItems`、`evidenceClaims` 和 `evidenceDeliverable`，让每日老板报告能直接看到事实核查包是否存在且厚度是否正常。
- `docs/ops-report.md` 已同步说明 evidence items/claims；验证 `npm run ops-report` 后 JSON/Markdown 都包含 evidence metrics，当前为 5 items、20 claims、`content-evidence-pack.md`。

## 2026-06-08 Content Delivery Gate

- 新增 `scripts/generate_content_delivery_gate.mjs` 与 `npm run content-delivery-gate`：读取 `dist/buyer-content-pack/manifest.json` 和四份 buyer deliverables，检查交付文件存在、manifest 四件套、seller-only 文件引用、敏感付款/账号数据请求、播放/订阅/收入/增长等结果承诺和 mojibake markers。
- 输出公开 `docs/content-delivery-gate.md` 与 ignored `dist/content-delivery-gate/manifest.json` / `checks.csv`；若 failed checks > 0，命令非零退出。
- `content-ops` 已在 `buyer-pack` 后接入 `content-delivery-gate`，当前内容侧流水线为 21 步；`content-health` 覆盖 delivery gate doc、checks CSV 和脚本源码。
- `ops-report` 的 Content Sales Lane 已显示 delivery gate pass/fail：当前 30 passed、0 failed。
- 验证：`node --check`、`npm run content-delivery-gate`、完整 `npm run content-ops`、`npm run ops-report` 和结构化 Node 门禁通过；当前 health checked files 57，mojibake/leak 均为 0。

## 2026-06-08 Content Order Completion

- 新增 `scripts/complete_content_order_delivery.mjs` 与 `npm run complete-content-order-delivery`：读取 `dist/content-orders/<order-id>/manifest.json`，确认四份 buyer deliverables 都存在，然后调用本地状态记录器把 ignored CRM 更新为 `fulfilled_waiting_feedback`。
- 输出 ignored `dist/content-order-completion/<order-id>.json/.md` 和 `latest.json`，作为私密 completion receipt；不发送消息、不收款、不上传、不保存敏感付款数据。
- `content-deal-desk` 的标准 proof script pack fulfillment command 已追加 completion 回写命令，避免交付完成后忘记更新 CRM。
- QA 验证：临时 `qa-complete-content-order` 先由 `fulfill-content` 生成四件套，再由 completion 命令写入 `fulfilled_waiting_feedback`；`content-crm` 可读取状态，公开 docs 不泄露 QA buyer/contact/notes。测试订单、completion receipt 和 private CSV 已恢复/删除。

## 2026-06-08 Content Customer Success

- 新增 `scripts/generate_content_customer_success.mjs` 与 `npm run content-customer-success`：把已交付内容订单转成 reviewer-ready feedback、testimonial permission 和 custom/weekly upsell 跟进草稿。
- 输入：ignored `dist/content-sales-crm/pipeline.csv` 中 `fulfilled_waiting_feedback` 行，以及 ignored `dist/content-order-completion/*.json` 完成回执；若只有完成回执、没有 prospect CRM 行，也会生成 receipt-only 跟进，避免手动成交漏单。
- 私密输出：`dist/content-customer-success/followups.csv`、`followup-drafts.md`、`manifest.json`；公开 `docs/content-customer-success.md` 只显示完成回执数、跟进数、due now 和 safety boundary。
- `content-ops` 已接入该步骤，当前内容侧流水线为 22 步；`content-health` 覆盖新脚本、公开说明和私密 follow-up drafts；`ops-report` 显示 customer success follow-ups / due now。
- QA 验证：临时 buyer 订单 -> `fulfill-content` -> `complete-content-order-delivery` -> `content-crm` -> `content-customer-success`，生成 1 条 due-now 私密跟进草稿；公开 doc 未泄露 QA buyer/contact/payment ref；测试私密 CSV、测试订单和 completion receipt 已恢复/删除。最终 `content-ops` 成功，health gate 检查 62 个文件，mojibake/leak 均为 0。

## 2026-06-08 Content Testimonials

- 新增 `scripts/update_content_testimonial_status.mjs` 与 `npm run record-content-testimonial`：把买家反馈 quote、permission、stage、context 写入 ignored `data/content-sales-crm/testimonials.csv`。
- 新增 `scripts/generate_content_testimonials.mjs` 与 `npm run content-testimonials`：把私密 testimonial rows 转成 `dist/content-testimonials/testimonial-bank.md`、`publish-candidates.md`、`testimonials.csv` 和 manifest。
- 安全逻辑：只有 `permission=explicit` 且 `stage=publish_ready`、无敏感数据、无 views/subscribers/revenue/growth 等 outcome claim 的 quote 才进入 publish candidate；其余进入 permission/review queue。公开 `docs/content-testimonials.md` 不暴露 buyer 名或 quote。
- `content-ops` 已接入该步骤，当前内容侧流水线为 23 步；`ops-report` 显示 testimonial private rows / publish candidates；`content-health` 覆盖新脚本、公开说明和私密 testimonial bank。
- QA 验证：临时 explicit/publish_ready quote 可进入私密 publish candidates；公开 docs 不泄露 QA buyer/quote；私密测试 CSV 已恢复/删除。最终 `content-ops` 成功，health gate 检查 69 个文件，mojibake/leak 均为 0。

## 2026-06-08 Content Outreach Send Receipts

- 新增 `scripts/complete_content_outreach_send.mjs` 与 `npm run complete-content-outreach-send`：以 `review_id` 读取 ignored `dist/content-outreach-review/review-board.csv` 和对应 send pack，记录“已人工审核并发送”的私密 receipt。
- 命令会调用 `record-content-sale` 把对应 creator/source 回写为 `sent_waiting_reply`，使用 send pack 的 follow-up date 或显式 `--next-due`，避免发送后忘记进入 CRM 跟进队列。
- 新增 `scripts/generate_content_outreach_send_log.mjs` 与 `npm run content-outreach-sends`：汇总 `dist/content-outreach-sends/*.json`，公开 `docs/content-outreach-sends.md` 只显示 send receipt 数、sent today、waiting reply；私密 `send-log.md/csv` 保留 creator 行。
- `content-ops` 已接入该步骤，当前内容侧流水线为 24 步；`ops-report` 显示 outreach send receipts / waiting reply；`content-health` 覆盖新脚本、公开说明和私密 send log。
- QA 验证：临时记录一个 reviewed send pack，生成 1 条私密 send receipt 并回写 CRM；公开 docs 未泄露 creator/review-id/QA notes；测试 override 和 receipt 已恢复/删除。最终 `content-ops` 成功，health gate 检查 74 个文件，mojibake/leak 均为 0。

## 2026-06-08 Content Reply Intake

- 新增 `scripts/intake_content_replies.mjs` 与 `npm run content-reply-intake`：读取 ignored `data/content-sales-crm/reply-inbox/*.txt|md|eml`，解析 `Source`、`Creator`、`Stage`、`Summary`、`Objection`、`Notes` 等字段。
- 写入 ignored `data/content-sales-crm/replies.csv` 和 `overrides.csv`，让 `content-crm`、`content-deal-desk`、`content-feedback` 自动接管回复；公开 `docs/content-reply-intake.md` 只显示计数和安全边界，不暴露原文。
- 安全策略：默认 reply 只进入 `replied_needs_response` 或 `qualified_needs_custom_pack`；只有 raw file 显式写 `Stage: paid_needs_fulfillment` 才视为待履约，脚本不自动确认付款。
- `content-ops` 已接入该步骤，当前内容侧流水线为 25 步；`ops-report` 显示 reply intake parsed/skipped；`content-health` 覆盖新脚本、公开说明和私密 parsed replies。
- QA 验证：临时 reply inbox 文件解析为 `qualified_needs_custom_pack`，CRM/deal desk 生成 1 条 active deal；公开 docs 未泄露 QA buyer/reply 原文；测试 inbox、private CSV 已恢复/删除。

## 2026-06-08 Concise Buyer Delivery

- `buyer-pack` 已新增 buyer-facing `START-HERE.md`，放在 `buyerDeliverables[0]`；内容包含 TL;DR、Next Action、What To Ignore At First 和 boundary，作为客户第一阅读文件。
- `fulfill-content` 会复制 `START-HERE.md`；delivery email 已改为要求客户先看 `START-HERE.md`，长脚本、workbench、evidence、audit 只作为参考。
- `content-delivery-gate` 已新增精简门禁：buyer deliverables 必须不超过 5 个，第一文件必须是 `START-HERE.md`，`START-HERE.md` 必须包含 TL;DR / Next Action 且不超过 220 words。
- `complete-content-order-delivery` 已把 `START-HERE.md` 纳入 expected deliverables，避免交付完成回写时漏掉客户 quickstart。
- QA 验证：`buyer-pack` 生成 5 个 buyer deliverables，delivery gate 45 checks / 0 failed；临时订单履约包含 `START-HERE.md`，完成回写 checkedDeliverables=5；测试订单、completion receipt 和 private CSV 已恢复/删除。最终 `content-ops` 成功，health gate 检查 78 个文件，mojibake/leak 均为 0。

## 2026-06-08 Custom Proof Delivery Completion

- 新增 `scripts/complete_custom_proof_delivery.mjs` 与 `npm run complete-custom-proof-delivery`：验证 `dist/custom-proof-pack` 或 `dist/custom-email-orders/<order-id>` 中的 `custom-proof-pack.md` 和 manifest，然后写入 ignored `dist/content-order-completion/<order-id>.json/.md`。
- 命令同时调用 `record-content-sale` 把 ignored CRM 状态写为 `fulfilled_waiting_feedback`，让 `content-customer-success` 能接管 feedback/testimonial/upsell 跟进。
- `fulfill-custom-email-orders` 的公开说明已补充交付后 completion 命令；`content-health` 已覆盖新脚本源码。
- QA 验证：临时 custom pack -> `complete-custom-proof-delivery` -> `content-customer-success` 生成 1 条 due-now 私密跟进草稿；测试私密 CSV、completion receipt 已恢复/删除，默认 custom pack 已恢复。最终 `content-ops` 成功，health gate 检查 63 个文件，mojibake/leak 均为 0。

## 2026-06-07 Custom Proof Pack

- 新增 `scripts/generate_custom_proof_pack.mjs` 与 `npm run custom-proof-pack`：按 `--niche`、`--platform`、`--buyer`、`--channel` 从当前 `data/latest.json` 选出 6 条最匹配的 proof-first episode，生成 `docs/custom-proof-pack.md` 和 ignored `dist/custom-proof-pack/`。
- 默认样张为 `AI video creators / YouTube and Bilibili`；已用 `ComfyUI workflow creators / Bilibili` 做参数化 QA，确认 orderId、niche、platform、6 条 selected episodes 和 buyer deliverables 正常。
- `content-ops` 已接入 `custom-proof-pack`、`content-sales`、`content-prospects` 和 `content-crm`，现在默认九步：content-audit、episode-workbench、full-script、buyer-pack、custom-proof-pack、content-listing、content-sales、content-prospects、content-crm。
- `content-listing` 的 $49 `trendfoundry-proof-custom` 履约说明已改为从 `dist/custom-proof-pack` 交付 `custom-proof-pack.md`，可选附加 full script、episode workbench、content audit。
- 安全边界：custom pack 不发送消息、不收款、不上传、不 build 前端；不包含 seller-only 文件；不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。

## 2026-06-07 Content Subscription Plan

- 新增 `scripts/generate_content_subscription_plan.mjs` 与 `npm run content-subscription`：读取 `data/latest.json` 和 content listing，把 $19/月 `trendfoundry-proof-weekly` 转成 4 周交付日历。
- 输出 `docs/content-subscription-plan.md`、ignored `dist/content-subscription-plan/calendar.csv`、`subscriber-emails.md` 和 `manifest.json`；每周包含 week start、delivery date、theme、lead episode、proof URL、proof asset、retention note、safety line 和 subscriber email。
- `content-ops` 已接入 `content-subscription`，现在默认十步：content-audit、episode-workbench、full-script、buyer-pack、custom-proof-pack、content-listing、content-subscription、content-sales、content-prospects、content-crm。
- 当前验证：4 周 delivery dates 为 2026-06-10、2026-06-17、2026-06-24、2026-07-01；四封 subscriber email subject 齐全；不发送、不收款、不上传、不请求 sensitive payment/account data，不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。

## 2026-06-07 Content Revenue Model

- 新增 `scripts/generate_content_revenue_model.mjs` 与 `npm run content-revenue`：读取 content listing、prospecting、CRM 和 subscription plan，生成 `docs/content-revenue-model.md`、ignored `dist/content-revenue-model/scenarios.csv`、`targets.csv` 和 `manifest.json`。
- 输出 conservative / base / stretch 三种情景，计算 reviewed/month、reply rate、paid rate、paid customers、one-time cash、new MRR 和 month-one cash；并给出 USD 500 / USD 2000 MRR 所需 weekly/custom/mixed subscriber 数量。
- `content-ops` 已接入 `content-revenue` 作为第十一步，并在 `docs/content-ops.md` 显示 base new MRR 和 base month-one cash。
- 当前基准情景：base new MRR USD 70，base month-one cash USD 92.5；这是 planning model，不是收益保证、投资建议或平台增长承诺。

## 2026-06-07 Content Feedback Loop

- 新增 `scripts/generate_content_feedback_loop.mjs` 与 `npm run content-feedback`：读取 ignored `dist/content-sales-crm/pipeline.csv`、可选 ignored `data/content-sales-crm/replies.csv` 和 `dist/content-revenue-model/manifest.json`，生成 `docs/content-feedback-loop.md`、ignored `dist/content-feedback-loop/learnings.csv`、`questions.csv` 和 `manifest.json`。
- 默认无私密回复时生成 3 条 fallback learning：`no_replies_yet`、`assumption_to_validate`、`revenue_model_gap`；当前 pipeline rows 为 20、private reply rows 为 0、feedback questions 为 4。
- `content-ops` 已接入 `content-feedback` 作为第十二步，并在其后接入 `content-close`；现在默认顺序为 `content-audit` -> `episode-workbench` -> `full-script` -> `buyer-pack` -> `custom-proof-pack` -> `content-listing` -> `content-subscription` -> `content-sales` -> `content-prospects` -> `content-crm` -> `content-revenue` -> `content-feedback` -> `content-close`。
- 安全边界：只做本地学习，不发送消息、不发帖、不收款、不 build 前端、不请求敏感支付/账号数据、不公开私密 reply 行、不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
- 验证：`node --check scripts\generate_content_feedback_loop.mjs`、`node --check scripts\run_content_ops.mjs`、`npm run content-ops` 和 Python 门禁通过；manifest 显示 learningCount 3、questionCount 4、privateReplyRows 0，当前 run JSON 十三步全部 success。

## 2026-06-07 Content Close Pack

- 新增 `scripts/generate_content_close_pack.mjs` 与 `npm run content-close`：读取 ignored `dist/content-sales-crm/pipeline.csv`、`dist/content-feedback-loop/questions.csv` 和 `dist/content-listing/products.json`，生成 `docs/content-close-pack.md`、ignored `dist/content-close-pack/today-close-queue.md`、`today-close-queue.csv`、`reply-capture-template.csv` 和 `manifest.json`。
- 作用：把 CRM 今日跟进、反馈问题和产品 SKU 合并成 5 条可人工审核的 daily close queue。每条私密行包含 creator、source、topic、proof URL、建议 SKU、草稿、一个反馈问题和 reply capture 行。
- 当前结果：selectedCount 5、cleanTextCount 5、needsCleanupCount 0；默认优先选择干净 YouTube 行，避开 Bilibili mojibake 线索，真实名单只留在 ignored `dist/content-close-pack/`。
- `content-ops` 已接入 `content-close` 作为第十三步；`docs/content-ops.md` 显示 Close pack: 5 selected, 5 clean, 0 needing cleanup。
- 安全边界：不发送消息、不发帖、不收款、不 build 前端、不请求敏感支付/账号数据、不在公开文档泄露 prospect 行、不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
- 验证：`node --check scripts\generate_content_close_pack.mjs`、`node --check scripts\run_content_ops.mjs`、`npm run content-close`、`npm run content-ops` 和 Python 门禁通过；确认公开 `docs/content-close-pack.md` 不含 prospect 名或 source URL，私密队列含 5 条可审核行。

## 2026-06-07 Content Health Gate

- 新增 `scripts/generate_content_health_gate.mjs` 与 `npm run content-health`：以 UTF-8 直接读取 `data/latest.json`、公开内容 docs、ignored CRM/prospect/close pack 文件，生成 `docs/content-health-gate.md`、ignored `dist/content-health-gate/checks.csv` 和 `manifest.json`。
- 目的：区分 PowerShell 控制台显示乱码与真实文件 mojibake，防止错误地修坏正常中文；同时检查公开 `docs/content-close-pack.md` 是否泄露 private prospect 名或 source URL。
- 当前结果：checkedFileCount 18、filesWithMojibakeMarkers 0、bilibiliItemCount 30、readableBilibiliCount 30、publicCloseDocProspectLeaks 0。
- `content-ops` 已接入 `content-health` 作为第十四步；健康门禁失败会让命令非零退出，阻止继续把损坏内容当作可售资产。
- 安全边界：只读文本并写本地报告，不发送消息、不发帖、不收款、不 build 前端、不公开私密 prospect 行、不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
- 验证：`node --check scripts\generate_content_health_gate.mjs`、`node --check scripts\run_content_ops.mjs`、`npm run content-health`、`npm run content-ops` 和 Python 结构化门禁通过；确认 Bilibili 样本中文真实可读。

## 2026-06-07 Content Sales Sequence

- 新增 `scripts/generate_content_sales_sequence.mjs` 与 `npm run content-sales`：读取 `dist/content-listing/products.json` 和 `dist/custom-proof-pack/manifest.json`，生成 `docs/content-sales-sequence.md`、`dist/content-sales-sequence/sequence.md`、`sequence.csv` 和 `manifest.json`。
- 输出 6 条待审核草稿，覆盖 warm email、Bilibili dynamic、YouTube community、LinkedIn/X、follow-up email 和 objection reply；每条都有 audience、status、subject/body 和 CTA。
- `content-ops` 已接入 `content-sales` 作为第七步，并在 `docs/content-ops.md` 显示 sales draft 数量和渠道。
- 安全边界：sales sequence 只生成草稿，不发送消息、不发帖、不收款、不请求 sensitive payment/account data，不包含 seller-only 文件，不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。

## 2026-06-07 Content Prospecting

- 使用 first-customers 原则补齐内容产品获客侧：先一对一、人工审核、从最相关的 Bilibili/YouTube creator public signals 里找潜在买家或反馈对象。
- 新增 `scripts/generate_content_prospecting.mjs` 与 `npm run content-prospects`：读取 `data/latest.json`，筛选最多 20 条 YouTube/Bilibili creator prospects，生成 ignored `dist/content-prospecting/prospect-board.md`、`prospects.csv`、`drafts/*.md` 和 `manifest.json`；提交的 `docs/content-prospecting.md` 只记录方法、数量、边界，不公开 prospect 名单。
- `content-ops` 已接入 `content-prospects` 作为第八步，并在 `docs/content-ops.md` 显示 prospect 数量和渠道。
- 验证：`node --check scripts\generate_content_prospecting.mjs scripts\run_content_ops.mjs`、`npm run content-ops` 和 Python 门禁通过；本地生成 20 条 prospect，渠道为 YouTube/Bilibili，状态均为 `draft_review_before_sending`，不发送、不发帖、不收款、不抓私密联系方式，不包含 seller-only 文件或高风险承诺。

## 2026-06-07 Content Sales CRM

- 新增 `scripts/generate_content_sales_crm.mjs` 与 `npm run content-crm`：读取 ignored `dist/content-prospecting/prospects.csv`，生成 ignored `dist/content-sales-crm/pipeline.md`、`pipeline.csv` 和 `manifest.json`；提交的 `docs/content-sales-crm.md` 只记录 CRM 方法、数量和边界，不公开 prospect 表。
- 新增 `.gitignore` 规则 `data/content-sales-crm/`，供以后手动回填 `overrides.csv` / `replies.csv`，记录 sent、reply、paid、not-fit 等状态和下一次跟进日期。
- `content-ops` 已接入 `content-crm` 作为第九步，并在 `docs/content-ops.md` 显示 CRM rows、due today、due this week。
- 当前验证：本地 CRM 20 行，due today 5，推荐动作均为安全白名单；不发送消息、不发帖、不收款、不抓私密联系方式，不请求 sensitive payment/account data，不承诺 views、subscribers、revenue、platform growth 或 buyer outcomes。
