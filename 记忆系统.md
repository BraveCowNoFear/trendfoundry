# TrendFoundry 记忆系统

## 项目原则

- 本文件和 `工作区.md` 是项目状态源，不依赖聊天上下文。
- 每次推进后更新：完成事项、关键决策、阻塞、下一步。
- 外部长期用户偏好仍以 `C:\Users\Clr\.codex\memories` 为准。

## 2026-06-07 初始化

- 勇敢牛牛授权自动选择赚钱方向并持续推进。
- 已清理检查：`C:\Users\Clr\.codex\memories` 中未发现旧限制类关键词相关残留；Graphiti episode 搜索也未返回相关结果。
- 已建立项目方向：TrendFoundry，卖给 AI/开发者视频创作者的趋势情报包。
- 已建立 MVP 文件结构和核心脚本。
- 已加入 Windows 日更脚本，目标是注册计划任务 `TrendFoundryDaily`。
- 账号/钱包/支付未作为前置阻塞；当前先产出可卖样品和销售页。

## 可复用判断

- 单纯批量生成视频内容风险高，且容易被平台降权。
- 更好的低依赖切入点是卖给创作者的源头情报、选题、结构化脚本和工作流模板。
- 公开数据源优先：GitHub API、HN Algolia、arXiv API；B 站/YouTube 后续优先使用官方、RSS 或已登录人工审核环境中的合规浏览。

## 运行日志

- `npm run daily` 已运行成功，采集 71 条机会，生成 12 条情报包。
- 本地站点已验证：桌面和 390px 手机宽度都无横向溢出。
- Windows 计划任务 `TrendFoundryDaily` 已注册成功，状态 Ready。
- 已补齐 `docs/launch-plan.md` 和 `docs/sales-page-copy.md`，可在账号可用时直接发布/收款。
- `scripts/run_daily.ps1` 已手动验证成功，日志写入 `logs/daily-20260607-024137.log`。
- 已初始化本地 Git 仓库并提交首版：`aced12e Initial TrendFoundry MVP`。
- 清理并忽略 Office 风格临时文件 `~$*`，避免样品仓库混入噪声。
- 已把展示页升级为可筛选/可搜索情报台，增加站内样品 brief、脚本、launch plan、sales copy 链接。
- 已按“产品视觉迭代闭环”做桌面/移动端与交互验证：桌面 12 卡、source 过滤、搜索过滤、移动 390px 单列、无横向溢出。
- 已验证 `http://localhost:4173` 和四个 `.md` 交付物链接均返回 200，Markdown content type 正确。
- 已接入 YouTube 与 Bilibili 公开信号源，并将 portfolio 配额调整为 GitHub 4、Bilibili 3、YouTube 2、HN 2、arXiv 1。
- 已重构采集收尾：失败条目进入 `errors`，不会混入可售卖 `items`；同 source/query 的历史成功结果会作为 cached fallback 保持日报可用。
- 2026-06-07 实测：采集 119 条，成品组合覆盖 GitHub/YouTube/Bilibili/HN/arXiv；Bilibili 部分关键词仍可能 412，已作为后续重构点。
- 已新增样品包导出器 `scripts/export_pack.mjs`，用于生成 `dist/trendfoundry-sample-pack/`。
- 已新增 `docs/outreach-playbook.md` 和 `docs/payment-platform-fields.md`，让产品从展示样品推进到可触达/可上架。
- 已加入无支付集成成交入口：站点和销售文案包含 `$9 sample pack` mailto 下单 CTA，使用业务邮箱 `rivan_Britain@outlook.com`。
- 已补齐每条机会的 `whyNow`、`demoSteps`、`thumbnailPrompt`、`limitation`，并导出到 `opportunities.csv`。
- 已生成 20 人首发外联板 `docs/outreach-board.md` / `dist/trendfoundry-sample-pack/outreach-board.md`，含 status 与 next follow-up。
- 已用本地 HTTP 预览导出样品包：`http://localhost:4174`，验证桌面/移动无横向溢出、source 筛选可用、mailto CTA 存在。
- 已新增极简设计系统 `docs/design-system.md`，并将站点生成模板重构为更克制的情报产品视觉。
- 已新增 GitHub Pages 发布配置 `.github/workflows/pages.yml`，目标将 `site/` 公开部署。
- 已创建公开 GitHub 仓库 `BraveCowNoFear/trendfoundry` 并推送 `main`。
- 已启用 GitHub Pages workflow 部署，在线演示 `https://bravecownofear.github.io/trendfoundry/` 验证返回 200。
- GitHub Pages 此前成功部署 run：`27080307584`，提交 `7284af1 Add no-backend lead capture path`。
- GitHub Pages 此前成功部署 run：`27080533257`，提交 `3c9dddc Harden Bilibili collection retries`；公开页 HTTP 与浏览器验证均通过。
- GitHub Pages 此前成功部署 run：`27080634271`，提交 `159ec31 Add quality risk scoring for creator signals`；公开页 HTTP 与浏览器验证均通过，无 review badge、无水印/解析风险词、桌面与 390px 移动端无横向溢出。
- GitHub Pages 此前成功部署 run：`27080752909`，提交 `468baa7 Add pricing tiers to public landing page`；公开页 HTTP 与浏览器验证均通过，pricing tiers 桌面三列、390px 移动端单列，无横向溢出。
- GitHub Pages 此前成功部署 run：`27080995586`，提交 `0ec9d55 Publish free public sample pack`；公开页、`public-sample.md`、`public-sample.csv` HTTP 验证 200，浏览器验证桌面/390px 移动端无横向溢出。
- 公开 HTTP 验证：`/`、`/daily-brief.md`、`/sales-page-copy.md` 均返回 200。
- 公开浏览器视觉验证：12 张卡片、Bilibili 筛选显示 3 张、mailto 下单入口存在、桌面和 390px 移动端无横向溢出、极简顶部无渐变。
- 已新增 GitHub Issue Form 作为无后端线索入口：`order-sample-pack.yml`，公开页 CTA 连接到样品包请求表单；因未登录访问会跳转 GitHub login，首屏保留 email order 作为无登录备选。
- 已验证无后端线索路径已上线：公开页包含 `Request on GitHub` 与 `Email order`，Issue Form URL 返回 200，GitHub 标签 `lead` / `sample-pack` / `qualified` / `paid` / `not-fit` 已创建。
- 已新增本地 lead CRM：`npm run leads` 从 GitHub Issues 同步样品包请求，生成被 Git 忽略的 `data/leads.json` 和 `docs/lead-pipeline.md`；当前实测同步 0 条线索，流程正常。
- 已新增本地线索回复草稿：`npm run leads` 同步线索后额外生成被 Git 忽略的 `docs/lead-replies.md`，按 stage 输出 GitHub/email 回复草稿；当前实测同步 0 条线索时生成安全占位，脚本不自动发送消息。
- 已新增本地订单履约脚本：`npm run fulfill` 调用 `scripts/fulfill_order.mjs`，根据 buyer/contact/channel/type 生成 `dist/orders/<order-id>/`；包含买家交付文件、`manifest.json`、`delivery-email.md`、`fulfillment-checklist.md`，但排除 `prospects.csv`、`outreach-board.md`、`latest.json` 等 seller-only 文件。
- 履约实测：`npm run fulfill -- --buyer="Test Buyer" --contact="test@example.com" --channel="https://youtube.com/@test" --type="sample-issue" --order-id="qa-test-sample"` 成功；Node 检查确认无 forbidden 文件、邮件草稿含买家信息、公开样品中文无 mojibake；`dist/` 被 Git 忽略。
- 已新增独立线索同步脚本 `scripts/run_leads.ps1`，并注册 Windows 计划任务 `TrendFoundryLeadSync`，每 2 小时同步一次；实测日志 `logs/leads-20260607-033110.log`，当前同步 0 条线索。
- 已改进 Bilibili 采集抗 412：每个查询之间默认等待 1.8 秒，412/429/5xx 最多重试 3 次并记录 `collectionAttempts`；2026-06-07 实测 `npm run daily` 成功，采集 123 条，Bilibili 成品 28 条，1 个查询重试 3 次后仍 412，缓存兜底继续可用。
- 已按视觉迭代闭环复验本地站点：桌面 1280px、移动 390px 均无横向溢出；Bilibili 筛选后只显示 3 张 Bilibili 卡片。
- 已新增内容质量控制：采集层输出 `qualityFlags` / `qualityRisk`，对 engagement bait、overclaim、template hype、rights risk、too broad 扣分；build 组合优先展示 normal 条目，review 只作补位；CSV 导出包含质量字段。
- 质量迭代实测：`npm run daily` 成功，采集 124 条，review 标记 13 条；本地页面 12 张展示卡无 review 标记，无水印/解析风险标题，桌面与 390px 移动端无横向溢出。
- 已新增公开页转化模块：3 档套餐（$9 sample issue、$19/mo weekly brief、$49/mo custom niche）和 “What arrives” 交付清单；`docs/sales-page-copy.md` 同步补齐 tier details。
- 转化模块视觉闭环实测：本地桌面 1280px 下 3 个 pricing tier 等高三列，390px 移动端单列无横向溢出，Bilibili 筛选仍显示 3 张对应卡片。
- 已新增公开免费样品：`docs/public-sample.md`、`site/public-sample.md`、`site/public-sample.csv`，只展示 top 3 机会，让访客无需登录 GitHub 即可查看格式和质量；完整 12 条仍保留为请求/付费交付。
- 公开样品实测：`npm run daily` 成功，采集 124 条；`public-sample.md` / `.csv` 本地 HTTP 返回 200，Node UTF-8 检查无 mojibake；浏览器验证桌面/390px 移动端无横向溢出，sample CTA 正常，Bilibili 筛选仍为 3 张卡。
- 已提交并推送本地订单履约工作流：`2eda286 Add local order fulfillment workflow`；`scripts/fulfill_order.mjs` 生成 buyer-only 订单目录，`docs/fulfillment.md` 记录履约边界。
- GitHub Pages 此前成功部署 run：`27081176164`，提交 `2eda286 Add local order fulfillment workflow`；公开页、`public-sample.md`、`public-sample.csv` HTTP 验证 200。
- 线上视觉闭环实测：Edge 渲染 `https://bravecownofear.github.io/trendfoundry/?v=2eda286`，桌面 1280px 与移动 390px 均无横向溢出，Request on GitHub 与 Email order CTA 可见，15 张卡片正常。
- 履约复验：`npm run fulfill -- --buyer="Test Buyer" --contact="test@example.com" --channel="https://youtube.com/@test" --type="sample-issue" --order-id="qa-test-sample-2"` 成功；订单目录只有 8 个预期文件，未包含 seller-only 文件。
- 已新增本地一对一外联草稿器：`npm run draft-outreach` 从最新 normal-quality YouTube/Bilibili 信号选择唯一创作者，生成 `dist/outreach-drafts/` Markdown/CSV 草稿；脚本只生成待审核草稿，不自动发送。
- 外联草稿实测：`node --check scripts/draft_outreach.mjs` 与 `npm run draft-outreach -- --limit=12` 成功，生成 12 份草稿；抽查确认草稿链接公开样品，状态为 `drafted_review_before_sending`，并包含不承诺播放量/收益的说明。
- 已将外联草稿生成接入 `npm run daily`，现有 Windows 计划任务 `TrendFoundryDaily` 会在刷新情报包后自动刷新本地外联草稿。
- 完整日更实测：`npm run daily` 成功串联 collect/build/export/draft_outreach，采集 124 条，构建 12 张卡，导出 13 个样品包文件，并生成 20 份本地外联草稿。
- GitHub Pages 此前成功部署 run：`27081350046`，提交 `537c75c Refresh daily run with outreach drafts`；公开页、`public-sample.md`、`public-sample.csv` HTTP 验证 200，source errors 为 0。
- 线上视觉闭环复验：Edge 渲染 `https://bravecownofear.github.io/trendfoundry/?v=537c75c`，桌面 1280px 与移动 390px 均无横向溢出，View free sample、Request on GitHub 与 Email order CTA 可见，15 张卡片正常。
- 已新增本地 ready-lead 履约器：`npm run fulfill-ready` 读取 `data/leads.json`，为 `paid` 线索生成本地订单目录；`-- --include-qualified` 仅用于人工批准的样品交付；脚本不发送消息、不上传文件、不改 GitHub 标签。
- ready-lead 履约实测：`node --check` 覆盖共享履约模块、手动履约脚本和 ready-lead 脚本；真实 `npm run leads && npm run fulfill-ready` 在 0 线索时生成空报告；临时 paid/qualified/new_lead 测试用 `--include-qualified` 生成 2 个订单、跳过 1 个 new_lead，订单目录无 seller-only 文件，测试数据已恢复。
- 已修正销售文案中的 buyer delivery 清单，移除不该交付给买家的 `prospects.csv`；新增 `npm run commerce` 生成三档 SKU 的支付平台字段、CSV/JSON 和手动 invoice 草稿。
- commerce 资产实测：`node --check scripts/export_commerce.mjs` 与 `npm run commerce` 成功，导出 3 个 SKU；Node 门禁确认 fulfillment 和 manual invoices 不含 seller-only 文件，并且 long description 含不承诺 views/subscribers/revenue 的说明；销售页旧的 PDF 交付承诺已改为 Markdown/CSV/HTML。
- GitHub Pages 此前成功部署 run：`27081594685`，提交 `d01be0f Add commerce listing exports`；公开 `sales-page-copy.md` HTTP 验证 200，确认无 `prospects.csv` 买家交付项、无 PDF 承诺。
- 已新增本地运营报告：`npm run ops-report` 汇总公开部署、情报包、lead stage、外联草稿、commerce SKU、订单目录和下一步动作；只读本地状态，不发送消息、不收款、不改 GitHub 标签。
- 运营报告实测：清理本地 QA 测试订单目录后，`npm run ops-report` 成功输出 `dist/ops-report/ops-report.md`；报告显示 source errors 0、lead count 0、outreach drafts 20、commerce products 3、prepared order directories 0。
- 已新增社交预览资产流程：`npm run social` 用本机 Edge/Chrome 渲染 1200x630 极简预览图到 `site/og-image.png`，`build.mjs` 在站点 head 写入 OG/Twitter meta，并在页面加入视觉预览区。
- 社交预览实测：`node --check`、`npm run social`、`npm run build` 成功；本地 Node 门禁确认 PNG 为 1200x630 且 HTML 含 OG/Twitter meta；本地和线上 Edge QA 均确认桌面 1280px/移动 390px 无横向溢出，视觉预览图加载正常。
- GitHub Pages 此前成功部署 run：`27081775883`，提交 `7a844d7 Add social preview asset`；公开页和 `og-image.png` HTTP 验证 200。
- 已新增本地一键运营流水线：`npm run operate` 串联 daily、commerce、leads、fulfill-ready、ops-report；若 `site/og-image.png` 缺失则自动补跑 social。`scripts/run_ops.ps1` 写入 `logs/ops-*.log`。该流水线不发送消息、不收款、不上传文件、不改 GitHub 标签。
- 一键运营实测：首次运行暴露 Windows `spawnSync` 调 npm 的包装问题，已改为显式 `cmd.exe /d /s /c` 固定命令并复验；`npm run operate` 成功串联全部步骤，输出 `dist/ops-run/latest-run.json`，最终 source errors 0、lead count 0、ready order 0、outreach drafts 20、commerce products 3。
- 已升级每日计划任务路径：`scripts/run_daily.ps1` 现在执行完整 `npm run operate`，日志写入 `logs/daily-ops-*.log`；`TrendFoundryDaily` 保持原任务名和 08:15 调度。
- 计划任务实测：`schtasks /Run /TN TrendFoundryDaily` 可启动完整 operate 流水线，任务结束后状态 Ready、Last Result 0，最新日志 `logs/daily-ops-20260607-045135.log`；LeadSync 仍保持每 2 小时同步线索。
- GitHub Pages 此前成功部署 run：`27081881130`，提交 `bb776f2 Add one-command operations pipeline`；公开页、`public-sample.md`、`og-image.png` HTTP 验证 200；线上 Edge QA 桌面 1280px/移动 390px 无横向溢出，12 张卡片、CTA、预览图和 0 source errors 正常。
- GitHub Pages 最新成功部署 run 已更新为：`27082360189`，提交 `2316ff9 Add scene-by-scene script deliverable`；公开页、`public-sample.md`、`public-sample.csv`、`ready-to-record-script.md`、`og-image.png` HTTP 验证 200；`npm run qa -- --online` 42/42 通过；线上 Edge QA 桌面 1280px/移动 390px 无横向溢出，12 张卡片、Open script/订单 CTA、1200x630 预览图和 0 source errors 正常。
- 已新增 QA 守门脚本：`npm run qa` 验证本地站点、OG 图、seller-only 交付边界、commerce SKU、临时履约订单、ops report 和 Windows 计划任务；`npm run qa -- --online` 额外验证公开 Pages 资源。
- QA 守门实测：`node --check scripts/qa.mjs`、`npm run qa`、`npm run qa -- --online` 均成功；线上模式 38/38 通过，并写入 `dist/qa/latest-qa.md/json`。
- 已将 QA 接入一键运营流水线：`npm run operate` 末尾运行 `npm run qa -- --skip-scheduler`，避免计划任务运行中检查自身 Last Result 造成误判。
- 接入后实测：`node --check scripts/qa.mjs`、`node --check scripts/run_operations.mjs`、`npm run qa -- --skip-scheduler`、`npm run qa -- --online` 和完整 `npm run operate` 均通过；`operate` 最新运行包含 daily、commerce、leads、fulfill-ready、ops-report、qa 6 个成功步骤。
- 已增强可售脚本交付物：`scripts/build.mjs` 现在为最高分机会生成 6-8 分钟 scene-by-scene `ready-to-record-script.md`，包含分镜表、demo checklist、recording outline、asset checklist、shorts hooks、publishing metadata 和 fact safety notes；`scripts/qa.mjs` 已加入脚本链接与三项内容守门，`npm run operate` 实测 QA 30/30 通过；本地 Edge 视觉闭环 1280px/390px 无横向溢出且 `Open script` CTA 可见。
- 已把公开 `ready-to-record-script.md` 纳入线上 QA：`npm run qa -- --online` 现在检查脚本 HTTP 200、scene-by-scene、asset checklist 和 fact safety notes，最新实测 46/46 通过。
- 已同步上架资产文案：`scripts/export_commerce.mjs` 和 `docs/payment-platform-fields.md` 已明确 scene-by-scene ready-to-record script；QA 新增 commerce 文案守门，`npm run commerce` 与 `npm run qa -- --online` 实测 47/47 通过。
- 已增强 QA/ops-report 证据保留：`npm run qa -- --online` 额外写 `dist/qa/latest-online-qa.json`，`--skip-scheduler` 写 `latest-ops-qa.json`，避免计划任务 QA 覆盖线上 QA 证据；`npm run ops-report` 现在展示 latest online QA、latest QA run 和最近 operate 步骤，且 QA 已检查 ops-report 的 QA Gate 摘要。实测线上 QA 48/48、运营 QA 32/32 通过。
- 已增强买家交付边界：`scripts/export_pack.mjs` 的 sample pack manifest 拆分 `buyerDeliverables` / `sellerOnlyFiles`；`scripts/lib/fulfillment.mjs` 的订单 manifest 增加 `primaryValue`，delivery email 明确 scene-by-scene script 的内容与用途。QA 新增 manifest 分类、履约 manifest primary value、履约邮件 scene-by-scene 文案检查；`npm run qa -- --online` 实测 52/52 通过。
- 已增强 GitHub Issue Form 线索入口：表单明确付费包包含 scene-by-scene ready-to-record script，新增 preferred delivery route 与 safety acknowledgement；`scripts/sync_leads.mjs` 解析 delivery route 并在 pipeline/reply drafts 中展示，回复草稿同步 scene-by-scene 文案且提醒不要公开提交支付卡/私密信息。`npm run leads` 实测同步 0 条线索正常，`npm run qa -- --online` 实测 56/56 通过。
- 已新增首发传播资产流水线：`scripts/generate_launch_assets.mjs` / `npm run launch-assets` 生成 5 份本地待审核草稿（X、LinkedIn、Bilibili dynamic、GitHub pinned comment、warm email），接入 `npm run operate`；`docs/launch-posts.md` 可提交作为当前首发文案源。曾发现 launch assets source mix 用 `latest.items.slice(0,12)` 导致 `github 12`，已改为复用 4/3/2/2/1 portfolio quota；中文动态文案改用 Unicode 转义避免 mojibake。`npm run qa -- --online` 实测 62/62 通过。
- GitHub Pages 最新成功部署 run 已更新为：`27082884010`，提交 `b984073 Add manual launch asset pipeline`；公开页、`public-sample.md`、`ready-to-record-script.md`、`og-image.png` HTTP 验证 200；线上 Edge QA 桌面 1280px/移动 390px 无横向溢出，12 张卡片、Open script/订单 CTA 和 1200x630 预览图正常。
- GitHub Actions 云端运营流水线已修正为直接部署 Pages：由于 `GITHUB_TOKEN` 机器人提交不会触发独立 Pages workflow，`.github/workflows/daily-ops.yml` 现在在同一 run 中执行 configure/upload/deploy Pages。手动 run `27083053568` 成功完成 operations、提交刷新结果 `9710a60 Run scheduled TrendFoundry operations` 并部署公开站点；`npm run qa -- --online` 68/68 通过；线上 Edge 视觉闭环确认桌面 1280px/移动 390px 无横向溢出、12 张卡片和核心 CTA 可见、1200x630 预览图正常。
- 已新增 SEO 长尾获客资产：`scripts/build.mjs` 自动生成 `site/topics/ai-video-ideas.html`、`github-ai-projects.html`、`bilibili-ai-topics.html`、`youtube-ai-workflows.html`、`developer-trend-brief.html`，并输出 `robots.txt` / `sitemap.xml`；首页新增 Search pages 区块，把搜索流量导向样品包、脚本和下单 CTA。`scripts/qa.mjs` 已新增 SEO 守门，`npm run qa` 80/80 通过。视觉闭环用 Python Playwright + Edge 验证首页和主题页在桌面 1280px、移动 390px 均无横向溢出；曾发现移动主题页被长技术标题撑宽，已通过移动端 CTA 单列、全局断词和 overflow 防护修复。
- SEO 长尾页已上线验证：提交 `f1486f6 Add SEO landing pages for creator search traffic` 触发 Pages run `27083399504` 成功；`npm run qa -- --online` 95/95 通过，公开 `sitemap.xml` 与 `topics/ai-video-ideas.html` 均返回 200。Python Playwright + Edge 线上视觉闭环确认首页/主题页在 1280px 与 390px 均无横向溢出，首页 5 个 topic links、主题页 8 张卡和核心 CTA 可见。
- 已新增订阅/复访资产：`scripts/build.mjs` 现在生成 `site/feed.xml` 与 `site/feed.json`，首页 Subscribe 区块链接 RSS/JSON Feed；Feed 每次构建输出 top 12 机会、proof link、hook、demo angle、limitation 和订单 CTA，便于创作者订阅更新。`scripts/qa.mjs` 已新增 Feed 守门，本地 `npm run qa` 88/88 通过。曾发现 cached fallback 文案重复追加，已在 `scripts/collect.mjs` 增加清理逻辑；`data/latest.json` 复验重复标记为 0。
- 订阅/复访资产已上线验证：提交 `a6a263e Add subscription feeds for creator opportunities` 触发 Pages run `27088765850` 成功；`npm run qa -- --online` 111/111 通过，公开 `feed.xml` / `feed.json` 均返回 200 且各含 12 条机会。Python Playwright + Edge 线上视觉闭环确认首页 Subscribe 区块在 1280px 与 390px 可见，RSS/JSON Feed 按钮正常且无横向溢出。
- 已更新 GitHub 仓库 Topics：保留 `ai-tools`、`bilibili`、`creator-economy`、`github-trending`、`newsletter`、`static-site`、`trend-intelligence`、`youtube`，新增 `ai-video`、`creator-tools`、`json-feed`、`rss`，增强 GitHub 搜索与订阅资产发现。
- 已新增公开 issue 归档：`scripts/build.mjs` 自动生成 `site/issues/<YYYY-MM-DD>.html`、`site/issues/latest.html`、`site/issues/index.html` 和 `docs/issues/<YYYY-MM-DD>.md`；首页新增 Archive 区块，把每日刷新沉淀为可检索、可复访、可建立信任的公开历史。`.github/workflows/daily-ops.yml` 已在 `git add -u` 之外显式 `git add docs/issues site/issues`，只新增安全归档目录，不提交 ignored lead/raw/log/dist。`npm run qa` 98/98 通过；本地 Edge/Playwright 视觉闭环确认 issue 页和归档索引桌面/移动端无横向溢出。
- 公开 issue 归档已上线验证：提交 `860f10d Add durable public issue archive` 触发 Pages run `27089007093` 成功；`npm run qa -- --online` 123/123 通过，公开 `issues/`、`issues/latest.html`、`issues/2026-06-07.html` 均可访问。Python Playwright + Edge 线上视觉闭环确认首页 Archive 区块、latest issue 12 张卡、archive index 在 1280px 与 390px 均无横向溢出。
- 已新增双语首页转化层：提交 `f0d66d1 Add bilingual landing page toggle` 触发 Pages run `27089286112` 成功；首页可在 EN/中文之间切换并用 `localStorage` 持久化，中文覆盖首屏、套餐、样品、交付清单、SEO/Feed/Archive、搜索筛选计数、机会卡片和 CTA。`npm run qa` 103/103 通过，`npm run qa -- --online` 128/128 通过；Playwright + Edge 本地与线上视觉闭环确认 1280px/390px 无横向溢出，刷新后仍为中文，0 搜索结果显示 `0 个可见机会`，截图在 `dist/qa/local-i18n-home-*.png` 与 `dist/qa/online-i18n-home-*-f0d66d1.png`。
- 已新增静态中文获客入口与首屏 source mix polish：提交 `09e6c34 Add static Chinese landing page` 新增 `/zh/`，提交 `7becba6 Polish landing page source mix messaging` 优化首页/中文入口的来源组合面板、周更情报定位和轻量下单文案，提交 `8df6662 Simplify source mix legend` 将首屏来源组合收敛成更安静的文字 legend；Pages run `27089829822` 成功。`npm run qa` 109/109 通过，`npm run qa -- --online` 138/138 通过；Playwright + Edge 本地与线上视觉闭环确认 1280px/390px 无横向溢出、`/zh/` 默认中文、source mix legend 渲染正常、Bilibili 筛选与 0 结果中文计数正常，截图在 `dist/qa/local-source-legend-*.png` 与 `dist/qa/online-source-legend-*-8df6662.png`。
- 已新增双语 public sample 资产：提交 `64507a5 Add bilingual public sample assets` 新增 `public-sample.en.md`、`public-sample.zh-CN.md`、`public-sample.en.csv`、`public-sample.zh-CN.csv`，并同步样品包 manifest、launch assets、commerce/fulfillment 文案、README 链接和 `/zh/` CTA；提交 `44472ef Clean English public sample titles` 清理英文样品中的中文标题混入。Pages run `27090187226` 成功。`npm run qa` 114/114 通过，`npm run qa -- --online` 145/145 通过；Playwright + Edge 线上确认 `/zh/` 在 1280px/390px 无横向溢出，中文/英文样品链接存在，HTTP 检查确认英文样品不含中文“为什么”标题。
- 已新增无登录下单页：提交 `78aff21 Add no-login order page` 新增 `site/order/index.html`，首页和 `/zh/` 均链接到 `/order/`；页面集中展示三档套餐、英文/中文 mailto 草稿、GitHub 备选入口和不收集公开支付信息的安全说明。Pages run `27090368455` 成功。`npm run qa` 121/121 通过，`npm run qa -- --online` 155/155 通过；Playwright + Edge 本地与线上确认 1280px/390px 无横向溢出、3 个 tier、2 个邮件草稿、至少 6 个 mailto CTA 和安全文案均正常，截图在 `dist/qa/local-order-*.png` 与 `dist/qa/online-order-*-78aff21.png`。
- 已新增本地付款回复草稿闭环：提交 `32879d1 Add payment reply draft workflow`，Pages run `27090835761` 成功。`scripts/draft_payment_reply.mjs` / `npm run payment-reply` 可在收到无登录邮件订单后生成 `dist/payment-replies/<order-id>/payment-reply.md`、`invoice-draft.md`、`payment-checklist.md` 和 `manifest.json`。脚本只写本地草稿，不发送邮件、不创建支付链接、不收款、不上传文件、不改 GitHub 状态；付款回复明确不要求 card numbers、private IDs、passwords、wallet seeds 或 payment credentials，并继续排除 `prospects.csv`、`outreach-board.md`、`latest.json`。`/order/` 新增 Payment reply 说明区，移动标题/副标题改为父容器 100% 宽度。本地 `npm run qa` 129/129 通过，线上 `npm run qa -- --online` 164/164 通过；Edge CDP 线上桌面 1280x1100 与移动 390x1100 验证 `/order/?v=32879d1` 无横向溢出，截图在 `dist/qa/online-payment-order-*-32879d1.png`。
- 已新增本地邮件订单 intake 自动化：提交 `a873b29 Add local email order intake workflow`，Pages run `27095892299` 成功。`scripts/lib/payment_reply.mjs` 抽出付款回复库，`scripts/intake_email_orders.mjs` / `npm run intake-email-orders` 可扫描 ignored `data/email-orders/` 中的 `.txt`、`.md`、`.eml` 订单文本，生成 `dist/email-order-intake/orders.json`、`pipeline.md` 和匹配的本地付款回复包。该步骤已接入 `npm run operate`，并修复 `run_operations` 写 `latest-run.json` 滞后一轮的问题，使 ops report 能看到本轮 `intake-email-orders: success`。验证：手动 custom order intake 生成 $49/month 回复包；默认空 inbox 稳定输出空管线；完整 `npm run operate` 成功，末尾 `qa --skip-scheduler` 132/132 通过；完整 `npm run qa` 137/137 通过；线上 `npm run qa -- --online` 172/172 通过；Edge CDP 确认首页和 `/order/` 的 1280px/390px 视口无横向溢出。
- 已新增已付款邮件订单履约自动化：提交 `6ff4a46 Add paid email order fulfillment workflow`，Pages run `27096347501` 成功。`scripts/fulfill_email_orders.mjs` / `npm run fulfill-email-orders` 读取 `dist/email-order-intake/orders.json`，只为 `paid_needs_fulfillment` 的本地邮件订单生成 buyer-only `dist/orders/<order-id>/`，未付款订单跳过。该步骤已接入 `npm run operate`，位于 `intake-email-orders` 之后、`fulfill-ready` 之前。验证：手动 paid/unpaid 两封订单邮件中只准备 1 个 paid weekly order，生成目录不含 `prospects.csv`、`outreach-board.md`、`latest.json`；`npm run qa` 143/143 通过；完整 `npm run operate` 成功且 `fulfill-email-orders: success`，末尾 `qa --skip-scheduler` 138/138 通过；线上 `npm run qa -- --online` 178/178 通过；Codex 内置浏览器线上确认首页和 `/order/` 在 1280px/390px 下无横向溢出，订单页 8 个 mailto CTA 正常，截图在 `dist/qa/online-email-fulfillment-*-viewport-6ff4a46.png`。
- 已新增订单页复制草稿转化优化：`scripts/build.mjs` 为 `/order/` 每个套餐卡新增 `Copy draft / 复制草稿`，prepared email 区新增英文/中文复制按钮；复制内容与 mailto 草稿一致。若浏览器不给 clipboard 权限，会插入并选中只读 fallback 文本框。`scripts/qa.mjs` 已检查 copyable drafts、`navigator.clipboard` 和 `copy-fallback`；`npm run qa` 144/144 通过。本地 Codex 内置浏览器确认桌面 1280px、移动 390px 无横向溢出，点击复制后受限剪贴板 fallback 含 `Subject: TrendFoundry order`，截图在 `dist/qa/local-copy-order-*-v2.png`。
- 已完成整站逻辑与视觉重构：首页优先展示真实 `signal-board.png` 产品看板、免费样品和机会筛选器，再进入定价/交付/SEO 复访入口；`npm run visuals` 从当前数据生成 1200x760 看板图，首页/中文页首屏使用该图和轻量 ticker 动画，移动端压缩首屏并露出下一段内容。`npm run qa` 已升级到 155/155，覆盖 signal board、auth 静态安全配置和无 secret 默认配置；Codex 内置浏览器本地确认 1280x720 与 390x844 首页无横向溢出，最终截图在 `dist/qa/home-*-redesign-final.png`。提交 `66579c6` 已上线，Pages run `27097479725` 成功，线上 QA 197/197 通过，线上视觉截图在 `dist/qa/online-home-*-redesign-66579c6.png`。
- 已把 signal board 接入日更自动化：`daily` 现在在 build 后运行 `scripts/generate_site_visuals.mjs`，并生成 `site/signal-board.meta.json` 记录当前 `data/latest.json` 的 `dataGeneratedAt` 与 top items。QA 已检查 daily 脚本包含视觉生成、本地 meta 与当前数据一致、线上 meta 可访问且 timestamp 匹配；`npm run daily` + commerce/launch/ops-report 后本地 QA 158/158 通过。提交 `47041d7` 已上线，Pages run `27097689285` 成功，线上 QA 202/202 通过。
- 已新增 free sample pack 下载包：`npm run free-pack` 生成 `site/trendfoundry-free-sample-pack.zip` 和 `.json` manifest，包含公开样品、双语 CSV、ready-to-record script、signal board 图和订单说明，排除 seller-only 文件。`daily` 已接入该步骤；首页首屏、样品区、中文入口和 `/order/` 均链接下载包。提交 `72848db` 已上线，Pages run `27098024358` 成功；`npm run qa -- --online` 218/218 通过，线上移动 390x844 视觉闭环确认首页首屏 `Sample pack` + `signal-board.png`、订单页首屏下载按钮/GitHub request 可见，均无横向溢出，截图在 `dist/qa/online-free-pack-*-edge-72848db.png`。
- 已新增 animated workflow proof：`npm run visuals` 现在生成 `site/signal-demo.svg`，首页和 `/zh/` 在免费样品后、机会列表前展示动态工作流；该 SVG 被纳入 sitemap 和 free sample ZIP。提交 `32ca33b` 已上线，Pages run `27098807147` 成功；`npm run qa -- --online` 225/225 通过，线上 390x844 审计确认无横向溢出、动画区渲染 335x189、首屏 H1 与 metrics 不裁切。用户已安排另一个 UI 设计师 agent 接管后续前端设计，本 agent 后续主责转向内容侧：选题质量、脚本、样品包、外联素材、交付物和运营流水线。
- 已新增内容侧审计工作台：`scripts/generate_content_audit.mjs` / `npm run content-audit` 读取当前 `data/latest.json` 组合，按 evidence、recordability、specificity、commercial handoff、risk 评分，输出 `docs/content-editorial-audit.md` 与 ignored `dist/content-audit/latest.json`。首次审计显示 12 条 portfolio 中 11 条 high-fit、0 条 weak demo，但 12 条都有 generic why-now、4 条有 generic hook；后续内容主线应优先把 top 6 的 opening hook / why-now 改成具体、可录屏、可交付的选题角度。当前工作树有 UI 设计师 agent 的前端改动，内容提交不混入这些文件。
- 已修复内容生成泛化问题：`scripts/collect.mjs` 现在按 GitHub pushed/stars/issues、Bilibili/YouTube 发布时间与播放/浏览、HN points/comments、arXiv 发布时间/摘要生成 source-specific why-now；`openingHook()` 和 `demoSteps()` 也变为按来源给出最小安装、tiny input 复现、争议聚类或论文小实验。长视频标题会归一化成 `ComfyUI AI 视频工作流`、`AI 视频工具栈`、`AI 视频提示词生成器` 等可读 topic。`scripts/generate_content_audit.mjs` 的 Next Content Workbench 会在 rewrite queue 为空时切换到 ready-to-record 动作。验证：`node --check`、`npm run collect`、`npm run content-audit` 通过；最新审计为 12 portfolio、11 high-fit、0 generic hook、0 generic why-now、0 weak demo、0 rewrite queue。未运行 `npm run build`，未提交 UI agent 的前端脏文件。
- 已新增 episode workbench：`scripts/generate_episode_workbench.mjs` / `npm run episode-workbench` 从当前 high-fit normal portfolio 生成 5 条 ready episode，输出 `docs/episode-workbench.md` 与 ignored `dist/episode-workbench/latest.json`。每条含 Bilibili opening、YouTube opening、Recording Proof、Buyer Handoff；当前队列为 hermes-agent、ragflow、gemini-cli、langchain 和 YouTube prompt generator。验证通过：脚本语法、生成命令、文档结构、5 条 JSON episode、无乱码/旧 generic 模板/英文 opening 中文句式混入/双句号。该文件是后续内容录制和付费交付的主工作台。
- 已新增完整成片脚本生成器：`scripts/generate_full_episode_script.mjs` / `npm run full-script` 从当前最高分 high-fit normal item 生成 `docs/full-episode-script.md` 与 ignored `dist/full-episode-script/latest.json`。当前脚本对象为 `NousResearch/hermes-agent`，包含 Script Contract、70-Second Cold Open、8 段 Scene-By-Scene Script、Recording Checklist、Buyer Delivery Notes、Shorts hooks、Publishing Metadata 和 sample pack CTA。验证通过：语法、生成命令、结构门禁、编码门禁、无旧 generic why-now、无收益/播放保证承诺、无中英标点叠加。未运行 build，未提交并行 UI agent 的前端文件。
- 已新增 buyer content pack：`scripts/generate_buyer_content_pack.mjs` / `npm run buyer-pack` 把 full script、episode workbench、content audit 包装成 `docs/buyer-content-pack.md` 和 ignored `dist/buyer-content-pack/`，后者含 README、delivery-email、manifest 和三份买家内容文件副本。manifest 只列 `full-episode-script.md`、`episode-workbench.md`、`content-editorial-audit.md` 为 buyer deliverables，并排除 prospects/outreach/raw/latest/leads/payment/account 数据。验证通过：语法、生成命令、6 个 dist 文件、交付边界、无乱码/收益承诺/支付凭证请求/双句号。未运行 build，未提交并行 UI agent 文件。
- 已新增内容侧一键运营：`scripts/run_content_ops.mjs` / `npm run content-ops` 默认串联 content-audit、episode-workbench、full-script、buyer-pack，输出 `docs/content-ops.md` 与 ignored `dist/content-ops/latest-run.json`。默认不联网刷新、不发送消息、不收款、不 build、不覆盖前端；`-- --refresh` 才会先跑 collect。验证通过：四步全部 exit 0，run JSON safety 为 sendsMessages=false / collectsPayment=false / buildsFrontend=false / includesSellerOnlyFiles=false，buyer deliverables 仍只有三份内容文件。该命令是 UI agent 并行时的内容侧主入口。
- 已新增 content product listing：`scripts/generate_content_listing.mjs` / `npm run content-listing` 读取 buyer content pack manifest，生成 `docs/content-product-listing.md` 与 ignored `dist/content-listing/`，含 products JSON/CSV、platform listing、manual invoices。SKU 为 USD 9 one-time proof script pack、USD 19/month weekly、USD 49/month custom；fulfillment 只交付 full-episode-script、episode-workbench、content-editorial-audit。验证通过：3 个产品、3 行 CSV、4 个 dist 文件、SKU/fulfillment/safety 精确门禁，无 seller-only 文件、乱码、高风险保证承诺或支付凭证请求。未运行 build，未提交 UI 文件。
- 已把 content-listing 接入内容侧一键运营：`npm run content-ops` 现在默认五步串联 content-audit、episode-workbench、full-script、buyer-pack、content-listing；`docs/content-ops.md` 显示三档 listing SKU，并提醒审核 `docs/content-product-listing.md` 后再发布/复制支付平台字段。验证通过：五步全部 success，run JSON safety 仍为不发送、不收款、不 build、不含 seller-only 文件。未运行 build，未提交 UI 文件。
- 已新增内容包本地履约命令：`scripts/fulfill_content_order.mjs` / `npm run fulfill-content` 读取 `dist/buyer-content-pack/manifest.json`，为外部确认付款后的买家生成 `dist/content-orders/<order-id>/`。交付物只包含 `full-episode-script.md`、`episode-workbench.md`、`content-editorial-audit.md`，另生成本地 `manifest.json`、`delivery-email.md`、`fulfillment-checklist.md` 供审核。`--payment-ref` 默认 `not-provided`，只有显式传入外部确认引用时才标记已确认付款；命令不发送、不收款、不上传、不请求密码、卡号、私钥或钱包种子。文档见 `docs/content-fulfillment.md`。
- 已新增 weekly subscription 逐周履约命令：`scripts/fulfill_content_subscription.mjs` / `npm run fulfill-content-subscription` 读取 ignored `dist/content-subscription-plan/calendar.csv`，按 subscriber/contact/channel/week/payment-ref 生成 ignored `dist/content-subscriptions/<order-id>/`。买家交付物为 `weekly-proof-pack.md` 和 `subscriber-email.md`；本地操作文件为 backup candidates、manifest、checklist。QA 订单 `qa-weekly-subscription` 验证通过：5 个预期文件、week=2、paymentExternallyConfirmed=true、无 seller-only 文件或高风险承诺；命令不发送、不收款、不上传。
- 已新增 weekly subscription 本地 CRM：`scripts/generate_content_subscription_crm.mjs` / `npm run content-subscription-crm` 读取 ignored `data/content-subscriptions/subscribers.csv`，生成公开 `docs/content-subscription-crm.md` 和 ignored `dist/content-subscription-crm/` 私密 due queue、commands、manifest、subscriber template。`content-ops` 已接入该步骤，当前默认 15 步；验证通过语法、完整 content-ops 和 Python 门禁，subscriber rows 0、ready today 0、payment review 0，health gate checkedFileCount 22、mojibake/leak 均为 0。该模块不发送、不收款、不上传、不 build 前端，不在 tracked docs 暴露 subscriber contacts。
- 已新增 weekly subscription 状态写入器：`scripts/update_content_subscription_status.mjs` / `npm run record-content-subscription` 在外部确认订阅/付款后 upsert ignored `data/content-subscriptions/subscribers.csv`，避免手工编辑 CSV；随后 `content-subscription-crm` 会生成 due queue 和履约命令。该命令只写本地私密状态，不发送、不收款、不上传、不 build 前端。
- 已新增 weekly subscription 交付完成回写器：`scripts/complete_content_subscription_delivery.mjs` / `npm run complete-content-subscription-delivery` 读取 ignored `dist/content-subscriptions/<order-id>/manifest.json`，更新 ignored subscriber status 的 `last_delivered_week`、`last_sent_at` 与 `next_delivery_date`，把“生成周包”闭环到下一期排队。
- 交付完成回写验证通过：临时 subscriber 记录 -> 周包生成 -> completion 回写 -> CRM 刷新，week 1 完成后下一期交付日来自当前 calendar 的 week 2（2026-06-24）；测试私密数据和测试订单目录已删除，最终 `content-ops` 15 步通过，health gate checkedFileCount 22、mojibake/leak 均为 0。
- 已新增 $49 custom proof pack 生产入口：`scripts/generate_custom_proof_pack.mjs` / `npm run custom-proof-pack` 可按 niche/platform/buyer/channel 从当前公开信号中生成 `custom-proof-pack.md`、delivery email 和 manifest。`content-ops` 默认接入该步骤，`content-listing` 的 custom SKU 已改为交付 `dist/custom-proof-pack/custom-proof-pack.md`，可选附加普通内容三件套。默认样张和参数化 QA 均通过安全/编码/交付边界门禁，未运行 build，未提交 UI 文件。
- 已新增内容产品销售序列：`scripts/generate_content_sales_sequence.mjs` / `npm run content-sales` 读取 content listing 与 custom pack manifest，生成 6 条 warm email、Bilibili dynamic、YouTube community、LinkedIn/X、follow-up 和 objection reply 草稿。`content-ops` 默认接入该步骤，run JSON 记录渠道与草稿数；验证通过语法、默认流水线和 Python 安全/编码/交付边界门禁。该序列只供审核后发送/发布，不自动发消息、不发帖、不收款。
- 已新增内容产品 first-customers prospecting：`scripts/generate_content_prospecting.mjs` / `npm run content-prospects` 从当前 YouTube/Bilibili public signals 生成最多 20 条本地 prospect board、CSV 和个性化草稿，真实名单留在 ignored `dist/content-prospecting/`，公开 `docs/content-prospecting.md` 只记录方法与边界。`content-ops` 默认接入第八步；验证通过语法、默认流水线和 Python 安全/编码/交付边界门禁。
- 已新增内容销售 CRM：`scripts/generate_content_sales_crm.mjs` / `npm run content-crm` 从 ignored prospecting 输出生成本地 sales pipeline、今日跟进队列和 manifest；公开 `docs/content-sales-crm.md` 不包含 prospect 表。`data/content-sales-crm/` 已加入 `.gitignore`，供以后手动回填 overrides/replies；`content-ops` 默认接入第九步。验证通过语法、默认流水线和 Python 安全/编码/CRM 边界门禁。
- 已新增 weekly subscription fulfillment：`scripts/generate_content_subscription_plan.mjs` / `npm run content-subscription` 把 $19/月 weekly pack 生成 4 周交付日历、proof episode、subscriber email 和 manifest；`content-ops` 默认接入该步骤。验证通过语法、默认流水线和 Python 安全/编码/订阅交付边界门禁，当前 4 个 delivery dates 为 2026-06-10、2026-06-17、2026-06-24、2026-07-01。
- 已新增内容收入模型：`scripts/generate_content_revenue_model.mjs` / `npm run content-revenue` 读取 product listing、prospecting、CRM 和 subscription plan，生成保守/基准/乐观情景、MRR targets 和 next sales actions；`content-ops` 默认接入该步骤。验证通过语法、默认流水线和 Python 安全/编码/收入模型边界门禁；当前 base new MRR USD 70、base month-one cash USD 92.5，仅作 planning model。
- 已新增内容反馈学习闭环：`scripts/generate_content_feedback_loop.mjs` / `npm run content-feedback` 读取本地 CRM pipeline、可选 ignored `data/content-sales-crm/replies.csv` 和 revenue manifest，生成 `docs/content-feedback-loop.md` 与 ignored learnings/questions/manifest。`content-ops` 默认接入为第十二步；当前无私密回复时输出 3 条 fallback learning、4 个反馈问题、privateReplyRows 0。验证通过语法、默认流水线和 Python 安全/编码/私密回复不外泄门禁；不发送、不发帖、不收款、不 build 前端。
- 已新增内容每日成交包：`scripts/generate_content_close_pack.mjs` / `npm run content-close` 读取 CRM pipeline、feedback questions 和 product listing，生成公开说明 `docs/content-close-pack.md` 与 ignored `dist/content-close-pack/` 私密五条 close queue。`content-ops` 默认接入为第十三步；当前 selectedCount 5、cleanTextCount 5、needsCleanupCount 0，优先选择干净 YouTube 行以避开 Bilibili mojibake。验证通过语法、完整 content-ops、安全/编码/公开文档不泄露 prospect 门禁；不发送、不发帖、不收款、不 build 前端。
- 已新增内容健康门禁：`scripts/generate_content_health_gate.mjs` / `npm run content-health` 用 UTF-8 读取 `data/latest.json`、内容 docs 和 ignored private queues，生成 `docs/content-health-gate.md` 与 ignored checks/manifest；`content-ops` 默认接入为第十四步。当前 checkedFileCount 18、filesWithMojibakeMarkers 0、bilibiliItemCount 30、readableBilibiliCount 30、publicCloseDocProspectLeaks 0；确认此前 PowerShell `Get-Content` 显示的 Bilibili/中文乱码是控制台假象，不是真实文件损坏。验证通过语法、完整 content-ops 和 Python 结构化门禁；不发送、不发帖、不收款、不 build 前端。
